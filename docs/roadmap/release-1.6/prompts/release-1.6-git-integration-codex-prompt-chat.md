Read `release-1.6-git-integration-codex-prompt.md` completely and execute it as the sole Release 1.6 Git integration authority after accepted WP14.
Reconcile the immutable 71-path WP14 candidate plus this governed two-file integration authority pair as exactly 73 staged paths, excluding the two out-of-band planning-definition files.
Only after every pre-staging gate passes, create `release/1.6-durable-experiment-evidence-foundation`, one exact conventional commit, post-commit and fresh-checkout proof, a normal push, and one open non-draft PR to `main`.
Do not modify candidate content, merge or auto-merge, mutate Release 1.6 issues/milestone/Project state, tag/release, delete branches, stage excluded files, or begin Release 1.7.
Finish with the required integration report and success marker only when the exact validated PR is ready for explicit human merge authorization.
