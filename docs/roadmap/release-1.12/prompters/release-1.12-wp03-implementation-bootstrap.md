# GPT-5.6 Terra Bootstrap — Release 1.12 WP03 Implementation

Execute:

`release-1.12-wp03-implementation-authority-terra.md`

**Selected execution model: GPT-5.6 Terra**

Repository:

`C:\projects\github\AIQuantTradingResearch`

Expected canonical base:

`a3fd52b96e5479e4d11824cfad46bd63d0435811`

WP03:

`#262 — GHCR Publication & Azure F1 Deployment Automation`

Binding exact CREATE-only allowlist:

```text
eng/azure-cli/r1.12-deployment/wp03-ghcr-azure-f1/cleanup-f1-reference.ps1
eng/azure-cli/r1.12-deployment/wp03-ghcr-azure-f1/deploy-f1-reference.ps1
eng/azure-cli/r1.12-deployment/wp03-ghcr-azure-f1/publish-ghcr-image.ps1
eng/azure-cli/r1.12-deployment/wp03-ghcr-azure-f1/verify-f1-reference.ps1
```

`GPT-5.6 TERRA WP03 MUTATION CONTRACT: ONLY WP03_AUTHORIZED_PATH_SET MAY BE MUTATED`

No other repository path may be changed.

Authenticated GHCR/Azure operations must use the interactive Windows operator handoff. Provide exact copy/paste commands, classify each mutation, state expected evidence and exit code, then STOP for returned stdout/stderr. Never request secrets in chat or transfer auth caches.

Required acceptance:

`RELEASE 1.12 WP03 — GHCR PUBLICATION & AZURE F1 DEPLOYMENT AUTOMATION: PASS`

After acceptance, publish exactly 4/4 paths in one PR.

PR merge is NOT authorized.

Do not close #262 or change Project/milestone lifecycle.

Terminal:

`RELEASE 1.12 WP03 — IMPLEMENTATION AUTHORITY COMPLETE`
