# Release 1.12 WP04 — Terra W8 Single-Converge Authority

**Selected execution model: GPT-5.6 Terra**

## Mission

Execute canonical WP04 hybrid **W8**, the final hybrid scenario, from the finally accepted W7 boundary.

Use one exhaustive convergence cycle. Do not return for ordinary disposable harness/evidence defects:

```text
recover canonical W8 contract
→ evaluate ALL gates
→ collect ALL local defects
→ repair ALL authorized disposable defects
→ invalidate affected evidence
→ fresh hashes/root/attempt
→ rerun complete W8
→ cleanup/finalize/reopen
→ self-reconcile ALL gates
→ repeat until ALL_PASS
```

This authority ends at a W8 acceptance candidate. It does **not** authorize final WP04 acceptance, publication, PR creation, issue closure, Project status mutation, milestone mutation, or WP05.

---

## 1. Accepted predecessor boundary

W7 final Luna reconciliation is authoritative:

```text
W7RunId=
initialize-daca2767d90a4c9292a27c0828684855

W7DurableRoot=
C:\Users\sabsf\AppData\Local\AIQuantTradingResearch\wp04\w7\e89a6d2dfa6b47d19e99bb0dfb0ab8cf

RunnerSHA256=
54449AFC3987D6EAE641A43E7ACE9BF9D63501D12854849025EDE99F10901983

RuntimeHarnessSHA256=
BBA1DF31778E77A5AEE908538025C8B6C6099E930747B218F8292A714CE3226F

ValidatorSHA256=
F5BA6129D8EE9527754DB141EB7A64161417403141108B6A6FD09196DC5780A9

ChildProbeSHA256=
7E042D70D984A4241F4CFB3AC7F36B542AD21D622619FAD585947E27324E9443

WrapperSourceSHA256=
61552F5FC0576B267F354F46E59CE124BBF1155310A1223DE799C556FA03C92C
```

W7 canonical accepted observations:

```text
QualificationResult=SUCCESS
ArchiveRetrieval=RETAINED
FreshExtraction=PASS
EvidenceCheckpoint=FAIL
FinalLifecycle=EVIDENCE_PRESERVATION_FAILED
RestoreOnlyCount=1
S2CallbackConfigured=true
S2CallbackInvocationCount=1
InjectedFaultIdentity=SyntheticCheckpointFailure
ErrorPrecedence=PASS
P01-P20=20/20 PASS
UnresolvedEvidenceReferences=0
RealExternalCalls=0
TrackedProductionMutations=0
ExternalMutations=0
```

W1-W7 are accepted or authorized carry-forward.

All historical W5/W6/W7 attempt identities are permanently consumed. Never reuse any.

---

## 2. Recover the canonical W8 contract

The exact W8 semantics MUST be recovered from retained project-local governing artifacts and fixture bytes before allocating any W8 attempt identity.

Search/reconcile at minimum:

```text
release-1.12-wp04-terra-hybrid-revised-v2-validation-authority.md
release-1.12-wp04-terra-exact-byte-sandbox-validation-authority.md
initialize-qualification.ps1 W8 lifecycle fixture
retained W1-W8 hybrid scenario definitions
accepted H1 lifecycle-remediation authority/evidence
accepted S2 checkpoint-seam authority/evidence
```

Print before execution:

```text
RecoveredW8Contract
RecoveredW8ContractSources
RecoveredW8ExpectedQualificationResult
RecoveredW8ExpectedArchiveRetrieval
RecoveredW8ExpectedFreshExtraction
RecoveredW8ExpectedCheckpoint
RecoveredW8ExpectedFinalLifecycle
RecoveredW8ExpectedRestoreOnlyCount
RecoveredW8ExpectedS2Behavior
RecoveredW8ExpectedErrorPrecedence
RecoveredW8AcceptancePredicates
```

Do not infer W8 by extrapolating from W5-W7.

If sources conflict or any acceptance-critical W8 semantic cannot be recovered unambiguously:

```text
W8_CONTRACT_RECOVERY=NOT_PROVEN
```

STOP before attempt allocation. Enumerate all missing/conflicting sources. Do not guess.

---

## 3. Scope

Authorized byte-changing scope:

```text
disposable/local-only W8 runtime harness
disposable/local-only W8 validator/evidence serializer
disposable child/interception probe if canonically required
other disposable local validation material strictly necessary for W8 convergence
```

Forbidden:

```text
frozen runner mutation
tracked/production source mutation
production wrapper/helper mutation
architecture/policy change
real external invocation
Azure call/mutation
GitHub call/mutation
Docker/GHCR call/mutation
Twelve Data call/configuration
secret-contract change
README mutation
publication
PR creation
WP04 issue/project/milestone lifecycle
WP05
staging
commit
push
```

---

## 4. Preflight before W8 identity allocation

Require:

```text
Windows PowerShell=5.1.26100.9444
ParserErrorCount=0
FrozenRunnerSHA256 exact
accepted W5/W6/W7 durable roots readable and unchanged
production wrapper/helper bytes unchanged
wrapper SHA exact
tracked modified scope remains exactly the two pre-existing WP04 paths
StagedPaths=0
GitDiffCheck=PASS
RealExternalCalls=0
```

Known pre-existing tracked WP04 paths, to be independently verified from Git:

```text
eng/azure-cli/r1.12-deployment/wp04-persistent-sqlite/initialize-qualification.ps1
eng/azure-cli/r1.12-deployment/wp04-persistent-sqlite/verify-persistent-sqlite-webapp.ps1
```

No new W8 attempt identity until all pre-identity gates pass.

---

## 5. Execute the recovered W8 scenario exactly

Use the byte-exact copied production wrapper and only the canonical local interception/fault mechanism required by the recovered W8 contract.

No real external invocation.

Do not broaden interception or fault injection.

Derive lifecycle/error results from actual production-wrapper execution under the governed local harness. Do not substitute hard-coded expected constants for observations.

If W8 canonically uses S2, retain exact configured/invoked counts and affected/unaffected operations. If W8 canonically does not use S2, retain explicit absence evidence.

If W8 canonically exercises restoration/error precedence, retain the exact descriptor/correlation and observation ordering required by the accepted H1 contract.

---

## 6. Individual W8 predicates

Recover the canonical W8 predicate matrix from retained authority. Do not force a predicate count from W6/W7 unless the canonical W8 contract actually defines that count.

Every final predicate record must contain:

```text
PredicateId
Requirement
Expected
Observed
Result
EvidenceReference
ValidationMethod
ObservationPhase
ReferenceResolved
```

Require all canonical predicates PASS and:

```text
UnresolvedPredicateEvidenceReferences=0
```

No literal/aggregate PASS disconnected from underlying evidence.

---

## 7. Durable evidence

The final W8 durable root must independently support final Luna reconciliation.

Retain/index at least:

```text
canonical W8 contract + sources
attempt identity/history
artifact hashes
WinPS/parser evidence
authority-entry repository state
wrapper/helper source/copy identity evidence
complete individual W8 predicate records
canonical scenario/fault observations
qualification result
archive retrieval state
fresh extraction state
checkpoint state
final lifecycle
RestoreOnly/restoration evidence
S2 configured/invoked/absence evidence as applicable
error-precedence evidence
governed interception/call ledger
real/unexpected external-call accounting
secret-hygiene evidence
cleanup evidence
post-run repository state
derived aggregate
manifest/index/hashes
durable reopen validation
```

Require after reopen:

```text
UnresolvedEvidenceReferences=0
MissingRequiredEvidenceClasses=0
```

---

## 8. Cleanup/finalization ordering

Preserve all evidence needed for reconciliation before disposable cleanup.

Require:

```text
scenario execution complete
→ durable preservation of required scenario evidence
→ cleanup attempt
→ cleanup complete
→ cleanup predicate evaluation
→ final ledger/index serialization
→ durable root reopen
→ resolve every acceptance reference
```

Do not grant cleanup PASS from pre-cleanup observations.

---

## 9. Mutation, hygiene, and error precedence

Final W8 evidence must prove:

```text
RealExternalCalls=0
UnexpectedExternalCalls=0
ExternalMutations=0
AuthorityIntroducedTrackedProductionMutations=0
StagedPaths=0
GitDiffCheck=PASS
SecretHygiene=PASS
README mutation=0
```

Error-precedence validation must be observation-only and prove the disposable harness/evidence machinery did not replace or mask the canonical production-derived lifecycle/error.

---

## 10. Single-converge retry loop

For every defect correctable entirely within authorized disposable W8 scope:

```text
evaluate ALL gates
collect ALL local defects
repair ALL local defects
rehash changed disposable artifacts
invalidate affected candidate evidence
consume any allocated failed W8 identity
fresh durable root
fresh never-used W8 identity after pre-identity gates pass
rerun complete W8
cleanup
finalize/reopen
self-reconcile ALL gates
```

Repeat internally until ALL_PASS.

Never reuse an allocated W8 identity.

Do not return for ordinary evidence-reference, serializer, fixture assertion, cleanup-accounting, or disposable interception defects.

---

## 11. Absolute stop boundaries

STOP and report every known blocker if completion requires:

```text
frozen runner change
tracked/production source change
new Luna architecture/policy decision
real external invocation
Azure/GitHub/Docker/GHCR/Twelve Data action
secret-contract change
README change
publication
PR creation
WP04 issue/project/milestone lifecycle
WP05
staging/commit/push
```

---

## 12. Final W8 self-reconciliation

Before returning, independently reopen the final W8 root and evaluate all:

```text
A CanonicalContractRecovery
B IdentityFreshness
C IndividualW8Predicates
D CanonicalFaultOrScenarioSemantics
E LifecycleAndErrorPrecedence
F DurableEvidenceAndReopen
G BoundaryMutationHygiene
H CarryForwardAndWP04Sequencing
```

Evaluate all A-H even after a non-PASS.

### H requires

```text
W1-W7 accepted/carry-forward remains valid
accepted W5/W6/W7 evidence unchanged
publication blocker remains unresolved
WP04 #263 remains OPEN
milestone #63 remains OPEN
WP05 remains NOT_STARTED
```

W8 success does NOT itself authorize publication or WP04 closure.

---

## 13. Success markers

Only if A-H all PASS and the complete canonical W8 predicate matrix is ALL_PASS:

```text
RELEASE 1.12 WP04 — W8 SINGLE-CONVERGE EXECUTION: PASS
RELEASE 1.12 WP04 — W8 CANONICAL CONTRACT: RECOVERED_AND_SATISFIED
RELEASE 1.12 WP04 — W8 INDIVIDUAL ACCEPTANCE PREDICATES: ALL_PASS
RELEASE 1.12 WP04 — W8 CANONICAL SCENARIO SEMANTICS: PASS
RELEASE 1.12 WP04 — W8 ERROR PRECEDENCE: PASS
RELEASE 1.12 WP04 — W8 DURABLE EVIDENCE REOPEN: PASS
RELEASE 1.12 WP04 — W8 EVIDENCE REFERENCES: ALL_RESOLVED
RELEASE 1.12 WP04 — W8 REAL EXTERNAL CALLS: 0
RELEASE 1.12 WP04 — W8 TRACKED PRODUCTION MUTATIONS: 0
RELEASE 1.12 WP04 — W8 EXTERNAL MUTATIONS: 0
RELEASE 1.12 WP04 — W8 ACCEPTANCE CANDIDATE: READY_FOR_FINAL_LUNA
RELEASE 1.12 WP04 — W1/W2/W3/W4/W5/W6/W7: ACCEPTED_OR_AUTHORIZED_CARRY_FORWARD
RELEASE 1.12 WP04 — HYBRID W1-W8 EXECUTION: COMPLETE_CANDIDATE
RELEASE 1.12 WP04 — PUBLICATION BLOCKER: UNRESOLVED
RELEASE 1.12 WP04 — WP04 #263: OPEN
RELEASE 1.12 WP04 — MILESTONE #63: OPEN
RELEASE 1.12 WP04 — WP05: NOT_STARTED
```

Do not claim final WP04 acceptance under Terra authority.

---

## 14. Required handoff

Return:

```text
RecoveredW8Contract
RecoveredW8ContractSources
RecoveredW8ExpectedQualificationResult
RecoveredW8ExpectedArchiveRetrieval
RecoveredW8ExpectedFreshExtraction
RecoveredW8ExpectedCheckpoint
RecoveredW8ExpectedFinalLifecycle
RecoveredW8ExpectedRestoreOnlyCount
RecoveredW8ExpectedS2Behavior
RecoveredW8ExpectedErrorPrecedence

W8RetryAttemptCount
ConsumedFailedW8AttemptIds
FreshCandidateW8AttemptId
W8DurableEvidenceRoot

RunnerSHA256
RuntimeHarnessSHA256
ValidatorSHA256
ChildProbeSHA256
WrapperSourceSHA256
WrapperCopyPreSHA256
WrapperCopyPostSHA256

WindowsPowerShellVersion
ParserErrorCount

W8PredicateResults
W8PredicateCount
W8PassedPredicateCount
W8FailedPredicateCount
W8UnresolvedPredicateEvidenceReferenceCount

QualificationResult
ArchiveRetrieval
FreshExtraction
EvidenceCheckpoint
FinalLifecycle
RestoreOnlyCount

S2CallbackConfigured
ExpectedS2CallbackInvocationCount
ObservedS2CallbackInvocationCount
CanonicalFaultIdentity
AffectedOperation
UnaffectedOperations
ErrorPrecedenceResult

DurableReopenResult
UnresolvedEvidenceReferenceCount
MissingRequiredEvidenceClassCount
CleanupResult

RealExternalCallCount
UnexpectedExternalCallCount
ExternalMutationCount
AuthorityIntroducedTrackedMutationCount
StagedPathCount
GitDiffCheckResult
SecretHygieneResult

SectionAResult
SectionBResult
SectionCResult
SectionDResult
SectionEResult
SectionFResult
SectionGResult
SectionHResult

CarryForwardResult
BoundaryBlocker
NextAuthorizedAction
```

On complete success:

```text
NextAuthorizedAction=GPT-5.6 Luna final read-only W8 and hybrid W1-W8 reconciliation; publication and WP04 lifecycle remain unauthorized
```
