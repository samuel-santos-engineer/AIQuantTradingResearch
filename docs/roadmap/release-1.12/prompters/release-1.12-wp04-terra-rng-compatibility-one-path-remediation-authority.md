# GPT-5.6 Terra — Release 1.12 WP04 RNG Compatibility One-Path Remediation Authority

**Authority state:** `READY`  
**Selected execution model:** **GPT-5.6 Terra**

## Model authority map

- **GPT-5.6 Luna** — contract, policy, architecture, reconciliation, acceptance criteria, governance.
- **GPT-5.6 Terra** — PRIMARY: implement and locally validate the one-path Windows PowerShell RNG compatibility correction.
- **GPT-5.6 Sol** — supporting analysis/synthesis only; never silently replaces Luna or Terra.

## 1. Governed starting state

Release:

**Phase 4 — Release 1.12 WP04: Persistent SQLite Initialization, Data Update & Recovery**

Issue:

`#263`

Accepted reconciliation decision:

```text
P1 — C1_ONE_PATH_RNG_COMPATIBILITY_CORRECTION
```

Current deployed runtime image remains:

```text
sha256:17da5a99fffa26bf0bc6dd9417d66400a7e0ee64492d24fa745728b4714c1a03
```

Current source candidate:

```text
9d08c6c09192b63b955f86feb3aba136657df3e6
```

This source candidate is superseded after remediation because a tracked governed helper changes.

Accepted Luna decisions:

```text
Repository change required: YES
Path count: 1
New source commit required: YES
New image required: NO
Current candidate finality: SUPERSEDED_AFTER_REMEDIATION
SCM basic auth: preserve false
Azure image/digest: preserve current exact digest
```

No mutation occurred under the Luna reconciliation.

## 2. Exact remediation allowlist

Exactly one tracked path is authorized:

```text
MODIFY eng/azure-cli/r1.12-deployment/wp04-persistent-sqlite/verify-persistent-sqlite-webapp.ps1
```

Path count:

```text
1
```

Operation count:

```text
1 MODIFY
```

No other tracked path is authorized.

## 3. Exact RNG correction

Replace the unsupported static API:

```text
[System.Security.Cryptography.RandomNumberGenerator]::Fill(...)
```

with the governed compatible pattern:

```powershell
$tokenBytes = New-Object byte[] 32
$tokenGenerator = [System.Security.Cryptography.RandomNumberGenerator]::Create()
try {
    $tokenGenerator.GetBytes($tokenBytes)
}
finally {
    $tokenGenerator.Dispose()
}
```

Preserve:

```text
256-bit cryptographic randomness
token byte length
token encoding behavior
token secrecy
HTTP header behavior
temporary-setting behavior
polling behavior
exact RunId behavior
restoration behavior
```

Do not alter token semantics beyond runtime compatibility.

## 4. Forbidden changes

Do not change:

- HTTP endpoint path;
- HTTP header name;
- token placement;
- token encoding unless byte-for-byte behavior requires a compatibility-preserving equivalent;
- polling interval;
- polling timeout;
- D3 settings;
- exact-run validation;
- persistence semantics;
- Azure lifecycle behavior;
- SCM/FTP settings;
- Dockerfile;
- container entrypoint;
- application/runtime code;
- schema;
- README.

## 5. Windows PowerShell binding validation

The authoritative compatibility gate is the actual interactive Windows PowerShell runtime used by the governed Azure workflow.

Require:

- PowerShell AST parse: 0 errors;
- token generation executes successfully;
- generated byte-array length: 32;
- encoded token non-empty;
- two consecutive generated tokens differ;
- no token value printed.

If the helper supports a local dry-run/synthetic mode, use it.

Do not contact Azure under this authority.

## 6. Security validation

Prove:

- RNG source is `System.Security.Cryptography.RandomNumberGenerator`;
- token remains cryptographically generated;
- `Get-Random` is not used as the sole security source;
- no static/hard-coded token;
- no token logging;
- no token persistence into evidence JSON;
- no token included in sanitized output;
- no environment dump introduced.

Required:

`RELEASE 1.12 WP04 — RNG CRYPTOGRAPHIC STRENGTH PRESERVATION: PASS`

## 7. Helper behavioral regression checks

Re-run the helper's local/synthetic validation coverage.

Require unchanged behavior for:

- run ID handling;
- phase handling;
- HTTP URL construction;
- `X-WP04-Evidence-Token` header construction;
- 5-second polling interval;
- 180-second maximum;
- selected retryable not-ready status;
- exact RunId equality;
- eleven-field payload validation;
- malformed response failure;
- stale/wrong-run failure;
- timeout failure;
- restoration failure;
- no direct SQLite access;
- no Kudu VFS evidence retrieval.

## 8. Static gates

Require:

```text
PowerShell AST errors: 0
Gitleaks: pass
git diff --check: pass
exact tracked diff: 1 authorized path
unauthorized tracked paths: 0
```

Also scan the helper for:

```text
RandomNumberGenerator.Fill
```

Expected remaining active uses:

```text
0
```

Scan for compatible RNG use and confirm `RandomNumberGenerator.Create()` is present in the token-generation path.

## 9. Docker/image decision preservation

Do not build or publish a new image.

Luna already determined:

```text
NEW IMAGE REQUIRED: NO
```

Binding evidence:

```text
eng/ excluded by .dockerignore
Dockerfile copies only src/, python/, and container/
```

Therefore the currently deployed exact image digest remains the runtime candidate:

```text
sha256:17da5a99fffa26bf0bc6dd9417d66400a7e0ee64492d24fa745728b4714c1a03
```

This authority does not re-evaluate that architecture decision unless repository evidence contradicts it. If contradiction is found, STOP.

## 10. Git mutation boundary

This authority permits local working-tree modification and local validation only.

It does **not** authorize:

- staging;
- commit;
- push;
- Docker build;
- GHCR publication;
- Azure mutation;
- D3 settings;
- restart/redeploy;
- PR creation;
- issue closure;
- Project #2 mutation;
- milestone mutation.

A separate Terra publication authority is required after local success.

## 11. Azure-state preservation

Do not mutate Azure.

Preserve:

```text
SCM basic auth: false
FTP basic auth: false
Configured image:
  sha256:17da5a99fffa26bf0bc6dd9417d66400a7e0ee64492d24fa745728b4714c1a03
No D3 temporary settings applied
No qualification restart/redeploy performed
```

## 12. Stop conditions

STOP if:

- any second tracked path is required;
- Windows PowerShell still cannot execute token generation;
- cryptographic RNG strength cannot be preserved;
- token length/encoding semantics change unexpectedly;
- helper behavior outside RNG generation changes;
- `eng/` is discovered to affect Docker image contents contrary to Luna finding;
- Azure contact would be required;
- direct SQLite access would be introduced;
- Kudu VFS evidence retrieval reappears.

Do not widen scope.

## 13. Exact mutation accounting

Authorized:

```text
Repository working tree:
  1 modified tracked path
```

Expected zero:

```text
Git staging: 0
Git commits: 0
Git pushes: 0
Git tags: 0
Docker builds: 0
GHCR publications: 0
Azure mutations: 0
Provider mutations: 0
PR mutations: 0
Issue mutations: 0
Project #2 mutations: 0
Milestone mutations: 0
```

## 14. Required return evidence

Return:

- exact changed path;
- exact old/new RNG API summary;
- Windows PowerShell runtime/version;
- AST result;
- token-generation execution result;
- 32-byte length proof;
- two-token inequality proof;
- token secrecy proof;
- synthetic/helper regression test result;
- active `RandomNumberGenerator.Fill` scan result;
- compatible RNG scan result;
- Gitleaks result;
- direct-SQLite scan result;
- Kudu-VFS active-path scan result;
- `git diff --check`;
- Git status showing no staged changes;
- exact mutation accounting.

Do not return any actual token value.

## 15. Terminal markers

Full success requires:

`RELEASE 1.12 WP04 — RNG ONE-PATH REMEDIATION: PASS`

`RELEASE 1.12 WP04 — WINDOWS POWERSHELL RNG COMPATIBILITY: PASS`

`RELEASE 1.12 WP04 — RNG CRYPTOGRAPHIC STRENGTH PRESERVATION: PASS`

`RELEASE 1.12 WP04 — HTTP EVIDENCE HELPER BEHAVIOR PRESERVATION: PASS`

`RELEASE 1.12 WP04 — RNG REMEDIATION PATH GOVERNANCE: PASS`

`RELEASE 1.12 WP04 — RNG REMEDIATION MUTATION AUDIT: PASS`

`RELEASE 1.12 WP04 — TERRA RNG COMPATIBILITY REMEDIATION COMPLETE`

`RELEASE 1.12 WP04 — TERRA RNG REMEDIATION PUBLICATION AUTHORITY: READY`

Blocked:

`RELEASE 1.12 WP04 — RNG COMPATIBILITY REMEDIATION: BLOCKED`

`RELEASE 1.12 WP04 — EXECUTION AUTHORITY BLOCKED`
