# GPT-5.6 Luna Bootstrap — Release 1.12 WP03 Repository Path Designation

Execute:

`release-1.12-wp03-repository-path-designation-authority-luna.md`

**Selected execution model: GPT-5.6 Luna**

Repository:

`C:\projects\github\AIQuantTradingResearch`

Expected canonical `main`:

`a3fd52b96e5479e4d11824cfad46bd63d0435811`

WP03:

`#262 — GHCR Publication & Azure F1 Deployment Automation`

Expected lifecycle:
- #261 Closed/Done
- #262 Open/Todo
- milestone #63 Open, 6 open / 2 closed

This is a strict read-only path-designation authority.

Inspect:
- frozen Release 1.12 definition/execution-plan/file-manifest;
- current Release 1.12 deployment/control structure;
- `Dockerfile`, `.dockerignore`, `container/entrypoint.sh`;
- GitHub workflow conventions;
- GHCR/Azure automation conventions;
- historical `eng/azure-cli/r1.11-qualification/` evidence.

Do not mutate historical Initiative-1.11 evidence by default.

Freeze the exact literal:

`WP03_AUTHORIZED_PATH_SET`

with one operation per path.

Also output:

`WP03_EXPLICIT_DENY_SET`

and exact mutation counts.

No globs, directory-only entries, ellipses, or future unspecified files.

Explicitly decide:

`WP03 PATH DESIGNATION PUBLICATION REQUIREMENT: REQUIRED`

or

`WP03 PATH DESIGNATION PUBLICATION REQUIREMENT: NOT REQUIRED`

If PASS, emit:

`GPT-5.6 TERRA WP03 MUTATION CONTRACT: ONLY WP03_AUTHORIZED_PATH_SET MAY BE MUTATED`

`RELEASE 1.12 WP03 — REPOSITORY PATH DESIGNATION: PASS`

`RELEASE 1.12 WP03 — IMPLEMENTATION AUTHORITY: READY TO CREATE`

`RELEASE 1.12 WP03 — REPOSITORY PATH DESIGNATION AUTHORITY COMPLETE`

No staging, commits, pushes, PRs, merges, GitHub lifecycle changes, Docker/Azure/GHCR/provider mutations, package changes, or schema changes are authorized.
