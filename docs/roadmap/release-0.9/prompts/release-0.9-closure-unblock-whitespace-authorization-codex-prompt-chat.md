Read
`docs/roadmap/release-0.9/prompts/release-0.9-closure-unblock-whitespace-authorization-codex-prompt.md`
completely and apply exactly that narrow authorization.

Remove trailing whitespace only from the two explicitly authorized
Release 0.9 governance prompts, preserving all substantive content and
proving semantic equivalence.

Require both `git diff --check` and `git diff --cached --check` to pass
without weakening validation or changing Git configuration.

Then resume the existing `release-0.9-closure-unblock-codex-prompt.md`
from the staging-validation point through commit, isolated
fresh-checkout proof, push, and corrective PR inspection.

Stop before merge, do not alter Release 0.9 planning governance or start
Release 1.0, and return the complete resumed unblock report and final
decision.
