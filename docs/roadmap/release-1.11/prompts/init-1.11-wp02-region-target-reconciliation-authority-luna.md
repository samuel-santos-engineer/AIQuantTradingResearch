# INIT-1.11 WP02 — Region-Target Reconciliation Authority

## Model authority
- **GPT-5.6 Luna** — PRIMARY: contract, policy, architecture, definition, reconciliation, acceptance criteria, governance, read-only planning.
- **GPT-5.6 Terra** — implementation, validation execution, approved Git/GitHub/Azure mutations, lifecycle completion.
- **GPT-5.6 Sol** — supporting analysis only; never replaces Luna/Terra.

**Selected execution model: GPT-5.6 Luna.**

## Mission
Perform one narrow read-only reconciliation for WP02 of `Phase 4 - Initiative-1.11: Public Reference Deployment / Azure App Service F1 Feasibility Qualification`.

Change only the WP02 Azure target region from **Brazil South** to **West Central US** based on empirical App Service F1 quota evidence. Authorize no mutation.

## Canonical baseline
- PR #258 merged at `62a7e36eb3064982f3dbfd16b065f3cb8b75c524`.
- milestone #62 Open.
- #252 WP01 Closed/Done.
- #253 WP02 Open/Todo, Release unset.
- #254–#257 Open/Todo.
- `Initiative-1.11 ≠ Product Release 1.11`.
- Frozen candidate remains **Azure App Service Linux F1 + custom Docker + persistent `/home` + strict recurring infrastructure cost `$0.00`**.

## Binding empirical evidence
**Brazil South**
- App Service F1 VMs: `1 of 1`.
- Utilization: `100%`.
- Disposition: `SATURATED — reject as WP02 deployment target`.

**West Central US**
- App Service F1 VMs: `0 of 30`.
- Utilization: `0%`.
- Disposition: `AVAILABLE — select as WP02 deployment target`.

Reconcile the earlier Brazil South plan-creation failure as regional F1 quota exhaustion, not architectural infeasibility.

## Required decisions
Luna must record:
1. Brazil South is no longer the WP02 target.
2. West Central US is the WP02 target.
3. No service/SKU/container/persistence/cost architecture changes.
4. Existing personal Brazil South F1 workload is outside WP02 ownership and must remain untouched.
5. All other pre-existing personal Azure resources remain outside WP02 ownership unless separately authorized.
6. West Central US WP02 resources must be uniquely identifiable and independently cleanable.
7. All eight original WP02 probes remain mandatory.
8. WP02 remains blocked pending reconciliation/resumption, not NOT FEASIBLE.
9. WP03 remains unauthorized until WP02 PASS and #253 Closed/Done.

## Frozen constraints
Preserve App Service Linux F1, custom Docker, public HTTPS, persistent `/home`, `WEBSITES_ENABLE_APP_SERVICE_STORAGE=true`, `$0.00`, no Azure Files, no Container Apps, no mandatory ACR, no paid scaling, provider independence, no production architecture changes, no SQLite correctness claim in WP02, no Twelve Data proof in WP02, and no Phase B before WP06 FEASIBLE.

## Eight probes unchanged
1. F1 availability
2. custom Docker
3. public HTTPS
4. persistent `/home`
5. App Service restart persistence
6. container recycle persistence
7. image redeployment persistence
8. resource inventory

## Existing execution handoffs remain binding
Docker/WSL: **interactive `sabsf` PowerShell executes exact commands generated by Terra**. Terra stops for stdout/stderr and exit codes, evaluates evidence, and never infers success.

Azure CLI: **interactive `sabsf` PowerShell executes exact Azure CLI commands generated by Terra when the authenticated profile is required**. Terra states expected mutations, stops for sanitized evidence and exit codes, evaluates results, and never infers success. No credential/profile transfer into Codex.

## Authoritative binding
`WP02 AZURE TARGET REGION: West Central US`

`WP02 REJECTED REGION: Brazil South — F1 quota 1/1 saturated`

Required decision:
`AZURE F1 WP02 — REGION TARGET RECONCILIATION: WEST CENTRAL US SELECTED`

## Mutation audit
This Luna authority must perform exactly zero repository, Git, GitHub, Project, milestone, Azure, Docker, registry, Twelve Data, tag, or release mutations. Historical WP02 mutations are evidence only and are not re-counted.

`AZURE F1 WP02 — REGION RECONCILIATION MUTATION AUDIT: ZERO`

## Acceptance
If the evidence supports changing only the target region while preserving the frozen feasibility contract, emit:

`AZURE F1 WP02 — REGION TARGET RECONCILIATION: PASS`

Then authorize resumption of the existing Terra WP02 Execution Authority Amendment at the strict-$0/pre-mutation region gate using West Central US:

`AZURE F1 WP02 — TERRA EXECUTION RESUMPTION: AUTHORIZED`

Do not create a new WP02 contract. Do not close #253.

## Model marker
`GPT-5.6 MODEL MAP: LUNA=CONTRACT/POLICY/GOVERNANCE | TERRA=IMPLEMENTATION/EXECUTION | SOL=SUPPORTING ANALYSIS`

## Exact completion terminal
`AZURE F1 WP02 — REGION-TARGET RECONCILIATION AUTHORITY COMPLETE`

## Block
Block if West Central US availability evidence is invalid, the change requires altering frozen architecture/cost constraints, a paid dependency becomes required, or regional quota exhaustion cannot be distinguished from architectural infeasibility.

`AZURE F1 WP02 — REGION-TARGET RECONCILIATION AUTHORITY BLOCKED`
