Apply `init-1.11-wp02-interactive-azure-cli-execution-handoff-amendment-terra.md` using **GPT-5.6 Terra**, then resume the existing WP02 Execution Authority Amendment from its blocked Azure preflight.

Binding rules:
- interactive **`sabsf` PowerShell executes Azure CLI commands** generated by Terra;
- interactive **`sabsf` PowerShell executes Docker/WSL commands** under the existing Docker handoff;
- Terra provides exact copy/paste-ready commands, expected mutation counts, and required evidence;
- Terra STOPS after each command batch for my stdout/stderr and labeled exit codes;
- Terra independently evaluates returned evidence and never infers success.

Existing Azure authentication evidence: `Learn Azure subscription`, Enabled, default True; prior login/account commands exited 0. Do not re-login unless evidence shows the session expired.

Start with a READ-ONLY Azure preflight command block for subscription/provider/region/F1 availability evidence. Perform no Azure mutation until the strict-$0 pre-mutation gate is proven.

Then continue WP02 one governed batch at a time through deployment, all eight probes, resource disposition, mutation audit, acceptance, and—only after exact WP02 PASS—#253 Closed/Done lifecycle.

Never request secrets or Azure auth profile/token transfer.
