# INIT-1.11 WP05 — F1 Resource Envelope & Strict-$0 Qualification Authority

## Model authority
- **GPT-5.6 Luna** — contract, policy, architecture, definition, reconciliation, acceptance criteria, governance.
- **GPT-5.6 Terra** — PRIMARY: empirical resource/cost validation, approved Azure/GitHub mutations, evidence evaluation, cleanup, lifecycle.
- **GPT-5.6 Sol** — supporting analysis only; never replaces Luna/Terra.
**Selected execution model: GPT-5.6 Terra.**

## Mission
Execute WP05 (#256) for Phase 4 Initiative-1.11. Determine whether the proven Azure App Service Linux F1 reference candidate fits its resource envelope while introducing exactly `$0.00` recurring infrastructure cost.

Decisive required line:
`ACTUAL RECURRING INFRASTRUCTURE COST: $0.00`

## Baseline
#252–#255 Closed/Done; #256 Open/Todo; #257 Open/Todo; milestone #62 Open (baseline 2 open / 4 closed). `Initiative-1.11 ≠ Product Release 1.11`.
Published baseline: PR #258 merge `62a7e36eb3064982f3dbfd16b065f3cb8b75c524`.
Target: `WP05 AZURE TARGET REGION: West Central US`.

Carry forward: Linux F1/custom Docker/public HTTPS/persistent `/home`; SQLite qualified with DELETE mode (WAL not selected); Twelve Data connectivity and secret/failure isolation qualified; prior Azure probe resources cleaned; retained public GHCR evidence artifacts must be inventoried.

## Frozen strict-$0 boundary
Preserve F1 only, minimal plan/app footprint where fresh empirical deployment is necessary, `/home`, custom Docker, SQLite DELETE, free registry path, provider independence, and `$0.00`. No Azure Files, Container Apps, mandatory ACR, paid tier/scale, paid monitoring dependency, Azure SQL, paid Twelve Data upgrade, production redesign, or Phase B before WP06 FEASIBLE. Personal Azure resources remain untouched.

Any mandatory recurring infrastructure cost above zero fails strict-$0.

## Resource vs cost qualification
Separately qualify:
1. **Resource envelope:** sufficient for low-volume public recruiter/reference use.
2. **Cost envelope:** all mandatory infrastructure has actual recurring infrastructure cost exactly `$0.00`.

F1 quota, CPU/storage ceilings, cold starts, throttling, no SLA, and availability limitations may be accepted only as explicit reference/demo constraints.

## Interactive handoff
**`sabsf` PowerShell executes authenticated Azure CLI commands generated by Terra.**
If narrowly needed, **`sabsf` PowerShell executes Docker/WSL commands generated by Terra.**
For each batch Terra gives exact commands, READ-ONLY/CREATE/UPDATE/RESTART/DELETE/LOCAL DOCKER/COST-USAGE classification, expected mutations, exit-code capture, sanitized evidence requirements, then STOPS. Terra evaluates independently and never infers success. Never request payment-card information, full billing documents, credentials, tokens, or Azure CLI profiles.

## Phase A — governance/ownership preflight
Verify #256 Open/Todo, #255 Closed/Done, #257 Open/Todo, #62 Open, staging empty, untracked work untouched, intended `Learn Azure subscription`, West Central US target, no leftover initiative-owned Azure resources, and personal-resource exclusion.
`AZURE F1 WP05 — GOVERNANCE & OWNERSHIP PREFLIGHT: PASS`

## Probe 1 — F1 quota/free-tier evidence
Capture authoritative current subscription/control-plane evidence for West Central US F1 quota/usage, actual Free/F1 SKU, relevant subscription/resource/storage limits and useful free-tier consumption indicators. Subscription-specific quota overrides generic maximums.
`AZURE F1 WP05 PROBE 1 — F1 QUOTA & FREE-TIER EVIDENCE: PASS`

## Probe 2 — mandatory dependency cost inventory
Inventory App Service plan, Web App, persistent `/home`, image registry/distribution, SQLite, default HTTPS/DNS, diagnostics/logging, Twelve Data, and deployment/CI dependencies. Classify each required/optional/excluded, Azure/external, recurring-cost mechanism, approved reference cost, evidence, and disposition.
`AZURE F1 WP05 PROBE 2 — MANDATORY DEPENDENCY COST INVENTORY: PASS`

## Probe 3 — F1 reference resource envelope
Reuse WP02–WP04 evidence unless a specific gap requires fresh deployment. If fresh deployment is necessary, create only minimal owned West Central US F1 resources after declaring mutations.
Qualify container responsiveness, HTTPS, `/home`, SQLite DELETE compatibility, bounded provider behavior, no forced paid tier, and suitability for low-volume reference/demo use. No abusive stress testing or deliberate exhaustion of F1 CPU allowance.
`AZURE F1 WP05 PROBE 3 — F1 REFERENCE RESOURCE ENVELOPE: PASS`

## Probe 4 — persistent storage envelope
Reconcile WP02/WP03 with authoritative F1 storage limits. Record persistent path, intended DB/config storage needs, ceiling, reasonable headroom, bounded-growth requirement, and explicit non-production/unlimited disclaimer.
`AZURE F1 WP05 PROBE 4 — PERSISTENT STORAGE ENVELOPE: PASS`

## Probe 5 — compute/runtime envelope
Record authoritative/current F1 runtime limitations where available. Establish that the reference app can serve bounded requests and updates while disclosing CPU quota, cold start, throttling, availability and no-production-SLA limitations.
`AZURE F1 WP05 PROBE 5 — COMPUTE & RUNTIME ENVELOPE: PASS`

## Probe 6 — network/provider cost envelope
Prove Twelve Data outbound access and default App Service HTTPS require no paid Azure networking product; no private endpoint/NAT/paid load balancer is mandatory; provider use remains bounded by existing entitlement.
`AZURE F1 WP05 PROBE 6 — NETWORK & PROVIDER COST ENVELOPE: PASS`

## Probe 7 — registry/deployment cost envelope
Inventory retained GHCR evidence tags/digests; prove ACR is not mandatory; approved registry/deployment path adds no mandatory recurring infrastructure cost; distinguish external registry terms from Azure cost and record manual/CI assumptions.
`AZURE F1 WP05 PROBE 7 — REGISTRY & DEPLOYMENT COST ENVELOPE: PASS`

## Probe 8 — strict-$0 reconciliation
Produce final cost reconciliation for every mandatory component, distinguishing actual recurring infrastructure cost, optional unselected services, usage caps, external entitlements, cleaned transient Free/F1 probes, and unknowns.

PASS is forbidden if mandatory cost is nonzero or materially unknown.

Required exact line:
`ACTUAL RECURRING INFRASTRUCTURE COST: $0.00`

Then:
`AZURE F1 WP05 PROBE 8 — STRICT-ZERO-COST RECONCILIATION: PASS`

## Qualification
If both envelopes pass:
`AZURE F1 WP05 — F1 RESOURCE ENVELOPE: QUALIFIED`
`AZURE F1 WP05 — STRICT-ZERO-COST: QUALIFIED`

Carry forward F1 reference/demo-only status, quota/CPU/storage/cold-start/throttle/no-SLA limitations, SQLite single-instance/reference assumptions and DELETE mode, bounded provider/update behavior, and external free-entitlement assumptions.

Failures:
`AZURE F1 WP05 — F1 RESOURCE ENVELOPE: NOT QUALIFIED`
or
`AZURE F1 WP05 — STRICT-ZERO-COST: NOT QUALIFIED`

## Resource disposition
Default cleanup. If no fresh Azure resources were created, state so. Otherwise delete all WP05-owned resources, verify RG existence `false`, clean temporary Docker artifacts and account for retained GHCR artifacts. Never touch personal resources.
`AZURE F1 WP05 — RESOURCE DISPOSITION: PASS`

## Mutation audit
Report exact Azure RG/plan/app/config/delete/retain mutations; quota/cost/usage read queries separately; Docker/registry mutations; repository/Git; GitHub/Project/milestone; Twelve Data requests; tags/releases.
`AZURE F1 WP05 — MUTATION AUDIT: PASS`

## Acceptance
PASS only if all eight probes pass, both envelopes QUALIFIED, the exact `$0.00` line is supported, disposition/audit pass, and personal resources are untouched.
`AZURE F1 WP05 — RESOURCE ENVELOPE & STRICT-ZERO-COST: PASS`
Do not emit early. BLOCK for remediable evidence/auth/tooling issues; NOT QUALIFIED for empirical resource/cost failure.

## Mandatory lifecycle
Only after exact PASS: close #256; ensure Project #2 Status Done; avoid redundant Status mutation if automation already does it; verify #256 Closed/Done; leave #62 Open and #257 Open/Todo.
`AZURE F1 WP05 — GITHUB LIFECYCLE: CLOSED/DONE`

Then:
`AZURE F1 WP06 — FEASIBILITY ACCEPTANCE AUTHORITY: READY`

WP06 (#257) is **GPT-5.6 Luna primary**; Terra only performs explicitly authorized validation/cleanup there. Do not execute WP06 here.

## Model marker
`GPT-5.6 MODEL MAP: LUNA=CONTRACT/POLICY/GOVERNANCE | TERRA=IMPLEMENTATION/EXECUTION | SOL=SUPPORTING ANALYSIS`

## Terminals
Success:
`AZURE F1 WP05 — F1 RESOURCE ENVELOPE & STRICT-ZERO-COST QUALIFICATION AUTHORITY COMPLETE`
Blocked:
`AZURE F1 WP05 — F1 RESOURCE ENVELOPE & STRICT-ZERO-COST QUALIFICATION AUTHORITY BLOCKED`
Resource failure:
`AZURE F1 WP05 — F1 RESOURCE ENVELOPE: NOT QUALIFIED`
Cost failure:
`AZURE F1 WP05 — STRICT-ZERO-COST: NOT QUALIFIED`
