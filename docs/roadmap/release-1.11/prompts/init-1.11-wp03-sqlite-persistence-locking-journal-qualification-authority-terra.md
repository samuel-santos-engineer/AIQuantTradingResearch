# INIT-1.11 WP03 — Persistent SQLite Filesystem, Locking & Journal Qualification Authority

## Model authority
- **GPT-5.6 Luna** — contract, policy, architecture, definition, reconciliation, acceptance criteria, governance.
- **GPT-5.6 Terra** — PRIMARY: empirical implementation/probes, validation, approved Docker/registry/Azure/GitHub mutations, cleanup, lifecycle.
- **GPT-5.6 Sol** — supporting analysis only; never replaces Luna/Terra.

**Selected execution model: GPT-5.6 Terra.**

## Mission
Execute WP03 (#254) for `Phase 4 - Initiative-1.11: Public Reference Deployment / Azure App Service F1 Feasibility Qualification`.

Empirically determine whether SQLite on Azure App Service Linux F1 persistent `/home` satisfies reference-deployment persistence, locking, journal, and integrity requirements. Never infer SQLite correctness from WP02's successful marker-file persistence.

## Baseline
WP02 is COMPLETE/PASS; #253 Closed/Done. #254 Open/Todo. #255–#257 Open/Todo. Milestone #62 Open. `Initiative-1.11 ≠ Product Release 1.11`.

Target:
`WP03 AZURE TARGET REGION: West Central US`

WP02 proved F1/custom Docker/HTTPS/persistent `/home` through restart, recycle and redeployment, then cleaned all owned Azure resources. Existing personal Azure resources remain outside initiative ownership.

## Frozen constraints
Preserve Linux F1, custom Docker, persistent `/home`, `WEBSITES_ENABLE_APP_SERVICE_STORAGE=true`, strict `$0.00`, no Azure Files, Container Apps, mandatory ACR, paid scaling, Azure SQL, production architecture changes, Twelve Data proof, ML/trading/backtesting, or Phase B before WP06 FEASIBLE.

A NOT QUALIFIED SQLite result is valid.

## Interactive execution boundary
**`sabsf` PowerShell executes Docker/WSL commands generated by Terra.**
**`sabsf` PowerShell executes authenticated Azure CLI commands generated by Terra.**

For every handoff Terra must provide exact copy/paste-ready commands, operation classification, expected mutation counts, labeled exit codes where practical, and required sanitized evidence; then STOP. Terra evaluates returned stdout/stderr/results and never infers success. Never request credentials/tokens/profile files. User-executed mutations count normally.

## Phase A — governance / strict-$0 preflight
Verify #254 Open/Todo, #253 Closed/Done, milestone #62 Open, repository staging empty, control artifacts untouched, West Central US F1 availability, no leftover WP02 resources, minimal independently-cleanable WP03 footprint, and no known recurring cost above `$0.00`.

No Azure mutation before:
`AZURE F1 WP03 — GOVERNANCE & STRICT-ZERO-COST PREFLIGHT: PASS`

## Phase B — purpose-built SQLite probe
Create/reuse a deterministic probe image capable of:
- DB path under `/home`;
- schema initialize + deterministic insert/read;
- SQLite version;
- requested/actual journal mode;
- explicit transactions;
- held write lock;
- competing write with explicit busy timeout/result;
- `PRAGMA integrity_check`;
- non-secret diagnostics and process/container identity.

Do not modify production architecture. Use the existing free public GHCR path where suitable.

`AZURE F1 WP03 — SQLITE PROBE IMAGE: PASS`

## Phase C — minimal deployment
Create only a uniquely owned West Central US resource group if required, one Linux F1 plan, one custom-container Web App, persistent storage enabled, SQLite DB under `/home`, and no unauthorized resources.

`AZURE F1 WP03 — SQLITE QUALIFICATION DEPLOYMENT: PASS`

## Probe 1 — baseline
Create DB under `/home`; record exact path, SQLite version, initialize schema, commit deterministic rows, read back, observe DB/journal files, and require `PRAGMA integrity_check = ok`.

`AZURE F1 WP03 PROBE 1 — SQLITE BASELINE READ/WRITE: PASS`

## Probe 2 — App restart durability
Record deterministic state, restart App Service, recover, prove same committed state, add/read a post-restart record, integrity check.

`AZURE F1 WP03 PROBE 2 — APP RESTART DURABILITY: PASS`

## Probe 3 — container recycle durability
Cause a controlled recycle distinguishable from Probe 2. Prove same DB/prior rows, add/read recycle record, integrity check.

`AZURE F1 WP03 PROBE 3 — CONTAINER RECYCLE DURABILITY: PASS`

## Probe 4 — image redeployment durability
Deploy a distinguishable image revision/digest without deleting `/home`. Prove all prior state, add/read redeployment record, integrity check.

`AZURE F1 WP03 PROBE 4 — IMAGE REDEPLOYMENT DURABILITY: PASS`

## Probe 5 — locking/contention
Connection/process A begins and holds a write transaction. B attempts a competing write. Capture transaction type, busy timeout, exact SQLite busy/locked result/error, then release/commit A and prove deterministic subsequent B write/final state. Require no lost/partial writes and integrity `ok`.

`AZURE F1 WP03 PROBE 5 — SQLITE LOCKING & CONTENTION: PASS`

## Probe 6 — journal-mode matrix
Empirically evaluate at minimum `DELETE` and `WAL`. For each record requested mode, actual returned mode, files observed, write/read, contention behavior, relevant durability, integrity, and anomalies.

WAL is NOT qualified merely because SQLite returns `wal`. If locking/shared-memory/durability semantics are ambiguous or insufficiently proven on App Service persistent storage, classify WAL as NOT QUALIFIED and use a safer proven mode if available.

`AZURE F1 WP03 PROBE 6 — JOURNAL MODE MATRIX: PASS`

## Probe 7 — selected configuration repetition
Using the selected journal mode, execute bounded repeated deterministic create/update/read/commit operations plus controlled contention. Verify expected final state and integrity. This is qualification, not performance benchmarking.

`AZURE F1 WP03 PROBE 7 — SELECTED SQLITE CONFIGURATION: PASS`

## Probe 8 — final integrity/inventory
Run final integrity check, capture deterministic DB state, enumerate all WP03-owned Azure resources and retained registry artifacts, prove personal resources untouched, and state selected/rejected journal configurations.

`AZURE F1 WP03 PROBE 8 — FINAL INTEGRITY & INVENTORY: PASS`

## Qualification decision
If proven:
`AZURE F1 WP03 — SQLITE FILESYSTEM QUALIFICATION: QUALIFIED`

State qualified `/home` DB path pattern, journal mode, busy-timeout/locking expectations, single-instance/reference assumptions, rejected modes (especially WAL where applicable), and limitations carried to WP05/WP06.

If no configuration satisfies persistence/locking/integrity:
`AZURE F1 WP03 — SQLITE FILESYSTEM QUALIFICATION: NOT QUALIFIED`

Do not redesign architecture here.

## Resource disposition
Default cleanup. Retain for WP04 only if published governance permits, useful, and still `$0.00`; otherwise delete all WP03 Azure resources and verify owned resource group existence `false`. Clean temporary local Docker artifacts and account for GHCR artifacts.

`AZURE F1 WP03 — RESOURCE DISPOSITION: PASS`

## Cost
No known recurring infrastructure charge above `$0.00`; WP05 owns final initiative-wide qualification.

`AZURE F1 WP03 — ZERO-COST CONSTRAINT PRESERVED: PASS`

## Mutation audit
Report exact current-run counts for Azure RG/plan/Web App creation, config, restart, redeploy, deletion/retention; Docker images/containers; registry artifacts; temp files; repository/Git; GitHub/Project/milestone; Twelve Data; tags/releases. User handoff mutations count.

`AZURE F1 WP03 — MUTATION AUDIT: PASS`

## Acceptance
PASS only if persistence probes, contention, journal matrix, at least one QUALIFIED SQLite configuration, final integrity, disposition, zero-cost, and mutation audit all pass.

Exact marker:
`AZURE F1 WP03 — SQLITE PERSISTENCE & JOURNAL QUALIFICATION: PASS`

Do not emit early. Use BLOCKED for remediable execution/evidence issues and NOT QUALIFIED for empirical SQLite failure.

## Mandatory GitHub lifecycle
Only after exact WP03 PASS:
1. close #254;
2. ensure Project #2 Status Done;
3. do not redundantly mutate Status if issue-close automation already did;
4. verify #254 Closed/Done;
5. leave milestone #62 Open;
6. leave #255 Open/Todo.

`AZURE F1 WP03 — GITHUB LIFECYCLE: CLOSED/DONE`

Then:
`AZURE F1 WP04 — EXECUTION AUTHORITY: READY`

Do not execute WP04 here.

## Model marker
`GPT-5.6 MODEL MAP: LUNA=CONTRACT/POLICY/GOVERNANCE | TERRA=IMPLEMENTATION/EXECUTION | SOL=SUPPORTING ANALYSIS`

## Terminals
Success:
`AZURE F1 WP03 — SQLITE PERSISTENCE, LOCKING & JOURNAL QUALIFICATION AUTHORITY COMPLETE`

Blocked:
`AZURE F1 WP03 — SQLITE PERSISTENCE, LOCKING & JOURNAL QUALIFICATION AUTHORITY BLOCKED`

Empirical failure:
`AZURE F1 WP03 — SQLITE FILESYSTEM QUALIFICATION: NOT QUALIFIED`
