Execute `init-1.11-wp02-execution-authority-amendment-terra-codex-prompt.md` using **GPT-5.6 Terra**.

This amendment supersedes conflicting operational bindings in the older WP02 authority.

WP02 is GitHub issue **#253** under milestone **#62**.

First verify:
- PR #258 is actually merged and identify its merge commit;
- `origin/main` contains the four published Initiative-1.11 governance artifacts;
- WP01 marker remains PASS;
- #252 Closed/Done;
- #253 Open/Todo and Release unset;
- #254 remains the next Open/Todo WP.

Then execute the strict-$0 prerequisite gates:
- Azure control tooling;
- authentication/subscription;
- F1 availability/eligibility;
- current official Microsoft documentation;
- no paid supporting resource.

Run only the WP02 minimal execution probe:
F1 → custom Docker → HTTPS → persistent `/home` → app restart persistence → container lifecycle persistence → image redeployment persistence → resource inventory.

WP02 uses ordinary marker files only. Do not claim SQLite correctness. Do not call Twelve Data.

On exact PASS:
`AZURE F1 WP02 — APP SERVICE F1 EXECUTION PROBE: PASS`

then close #253 and ensure Project #2 Status is Done. Count automation truthfully and do not redundantly mutate Status if issue closure already does it.

Leave milestone #62 Open and #254 Open/Todo. Do not execute WP03.

Use minimum zero-cost resources; clean up by default, retaining only the minimum for WP03 if the published WP01 resource plan explicitly permits it.

Report exact mutations and finish with the exact COMPLETE, NOT FEASIBLE, or BLOCKED terminal.
