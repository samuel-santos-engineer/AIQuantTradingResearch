Execute `init-1.11-wp02-region-target-reconciliation-authority-luna.md` using **GPT-5.6 Luna**.

Read-only reconciliation only. Evidence:
- Brazil South F1 VMs = 1/1 (100% saturated).
- West Central US F1 VMs = 0/30 (available).

Preserve Azure App Service Linux F1 + custom Docker + persistent `/home` + strict `$0.00`. Protect all pre-existing personal Azure resources.

If reconciled, bind `WP02 AZURE TARGET REGION: West Central US`, emit the reconciliation PASS and Terra resumption markers, then the exact COMPLETE terminal.

Zero mutations. Do not close #253. Do not execute WP02 or WP03.
