Execute `init-1.11-wp03-sqlite-persistence-locking-journal-qualification-authority-terra.md` using **GPT-5.6 Terra**.

WP02 PASS/#253 Closed/Done. WP03 = #254 Open/Todo. Target West Central US.

Binding handoffs:
- `sabsf` PowerShell executes Docker/WSL commands.
- `sabsf` PowerShell executes authenticated Azure CLI commands.
For every batch give exact commands, classification, expected mutations, exit-code capture, required sanitized evidence, then STOP. Evaluate results independently; never infer success.

Independently qualify SQLite under persistent `/home`: baseline, restart, recycle, image redeployment, controlled locking/contention, DELETE-vs-WAL matrix, selected-mode repetition, final integrity/inventory, qualification decision, cleanup, zero-cost, mutation audit.

Only if empirically QUALIFIED emit:
`AZURE F1 WP03 — SQLITE PERSISTENCE & JOURNAL QUALIFICATION: PASS`

Only after PASS close #254 and ensure Project #2 Done without redundant mutation. Leave #62 Open and #255 Open/Todo. Then emit WP04 READY and exact COMPLETE terminal.
