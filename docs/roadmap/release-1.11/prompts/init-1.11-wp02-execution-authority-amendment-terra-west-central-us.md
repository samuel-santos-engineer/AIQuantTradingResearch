# INIT-1.11 WP02 — Execution Authority Amendment
## Azure App Service Linux F1 Execution Probe — West Central US

## Model authority
- **GPT-5.6 Luna** — contract, policy, architecture, definition, reconciliation, acceptance criteria, governance.
- **GPT-5.6 Terra** — PRIMARY selected execution model: implementation, validation execution, approved Docker/registry/Azure/GitHub mutations, evidence evaluation, cleanup, lifecycle completion.
- **GPT-5.6 Sol** — supporting analysis, synthesis, alternatives, exploratory/non-authoritative review; never silently replaces Luna/Terra.

**Selected execution model: GPT-5.6 Terra.**

---

## 1. Mission

Resume and complete **WP02 — Minimal Docker + App Service F1 Execution Probe** for:

**Phase 4 - Initiative-1.11: Public Reference Deployment / Azure App Service F1 Feasibility Qualification**

using the reconciled target:

`WP02 AZURE TARGET REGION: West Central US`

Prove empirically the frozen candidate:

**Azure App Service Linux F1 + custom Docker + persistent `/home` + strict recurring infrastructure cost `$0.00`.**

This is a resumed execution authority, not a new WP02 contract.

---

## 2. Canonical governance baseline

- Initiative governance PR #258 merged at `62a7e36eb3064982f3dbfd16b065f3cb8b75c524`.
- milestone #62 remains Open.
- #252 WP01 Closed/Done.
- #253 WP02 Open/Todo, Release unset.
- #254 WP03 Open/Todo.
- #255–#257 Open/Todo.
- `Initiative-1.11 ≠ Product Release 1.11`.
- Product Release 2.0 remains ML.
- No Phase B/public-reference implementation before WP06 FEASIBLE.

Published governance package remains authoritative under:
`docs/roadmap/initiatives/azure-f1-public-reference-deployment/`

---

## 3. Accepted prerequisite and reconciliation evidence

### Local tooling
Accepted:

`AZURE F1 WP02 — LOCAL TOOLING PREREQUISITES: PASS`

Evidence:
- Azure CLI 2.89.1 works in interactive `sabsf`.
- Docker Desktop Linux engine works in interactive `sabsf`.
- WSL 2 works there.
- deterministic Linux-container build/run proof passed and was cleaned.

### Azure authentication
Interactive `sabsf` Azure CLI authentication is valid for:
- subscription: `Learn Azure subscription`
- state: Enabled
- default: True.

Do not transfer Azure CLI profiles/tokens into Codex.

### Region reconciliation
Accepted Luna decision:

`AZURE F1 WP02 — REGION TARGET RECONCILIATION: PASS`

Binding evidence:
- Brazil South F1 = `1/1`, 100% saturated.
- West Central US F1 = `0/30`, available.
- Brazil South failure = regional quota exhaustion, not architectural infeasibility.

Binding target:

`WP02 AZURE TARGET REGION: West Central US`

Rejected target:

`WP02 REJECTED REGION: Brazil South — F1 quota 1/1 saturated`

Existing personal Brazil South and all other pre-existing personal Azure resources are outside WP02 ownership and MUST remain untouched.

---

# 4. Binding interactive execution handoffs

## Docker/WSL
**Interactive `sabsf` PowerShell executes Docker/WSL commands generated by Terra.**

For every Docker/WSL operation Terra MUST:
1. provide exact copy/paste-ready PowerShell commands;
2. identify expected local/registry mutations;
3. include labeled exit codes where practical;
4. state exactly what stdout/stderr/evidence must be returned;
5. STOP and wait;
6. independently evaluate returned evidence;
7. request only missing evidence if incomplete;
8. never infer success.

## Azure CLI
**Interactive `sabsf` PowerShell executes Azure CLI commands generated by Terra when the authenticated profile is required.**

For every Azure command batch Terra MUST:
1. state the WP02 gate/probe;
2. provide exact copy/paste-ready PowerShell commands;
3. resolve known values and generate safe unique names where required;
4. classify commands as READ-ONLY, CREATE, UPDATE, RESTART/REDEPLOY, or DELETE;
5. state expected Azure mutation counts before execution;
6. include labeled exit codes;
7. request sanitized stdout/stderr/evidence;
8. STOP and wait;
9. independently validate returned evidence;
10. never infer success.

Never request tokens, passwords, registry secrets, publish profiles, connection strings, or Azure CLI profile files.

User-executed commands count as real mutations.

---

# 5. Frozen constraints

Preserve:
- Azure App Service Linux F1;
- custom Docker;
- public HTTPS;
- persistent `/home`;
- `WEBSITES_ENABLE_APP_SERVICE_STORAGE=true`;
- strict recurring infrastructure cost `$0.00`;
- no Azure Files;
- no Container Apps;
- no mandatory ACR;
- no paid App Service SKU/scaling;
- provider-independent application architecture;
- no production architecture changes;
- no SQLite correctness claim in WP02;
- no Twelve Data proof in WP02;
- no ML/trading/backtesting;
- no Phase B before WP06 FEASIBLE.

---

# 6. Historical WP02 mutation baseline

Carry forward, but do not re-count as new mutations:

Azure:
- resource groups created: 1
- resource groups deleted: 1
- plans created: 0
- web apps created: 0
- Azure Files: 0
- ACR/Container Apps: 0

Docker/registry:
- local tooling probe image/container: created/run and cleaned
- one public GHCR WP02 probe image: pushed and retained

Repository/Git/GitHub/Project/milestone:
- mutations: 0

The retained GHCR probe image must remain explicitly accounted for.

---

# 7. Phase A — resumed governance/pre-mutation verification

Read-only verify:
- #253 remains Open/Todo;
- #254 remains Open/Todo;
- milestone #62 remains Open;
- repository staging remains empty;
- pre-existing untracked control artifacts remain untouched;
- West Central US remains the reconciled target;
- interactive subscription remains `Learn Azure subscription`;
- West Central US F1 quota remains sufficient before mutation;
- no owned WP02 Azure resources from the failed Brazil South attempt remain.

Required:

`AZURE F1 WP02 — RESUMED PREFLIGHT: PASS`

---

# 8. Phase B — strict-$0 pre-mutation gate

Before creating any West Central US resource, verify:
- target region = West Central US;
- App Service F1 quota is available;
- selected plan SKU is Free/F1;
- no paid dependency is required;
- no Azure Files;
- no Container Apps;
- no mandatory ACR;
- registry path remains compatible with `$0.00`;
- planned resources are minimal, uniquely named, and independently cleanable.

Required:

`AZURE F1 WP02 — STRICT-ZERO-COST PRE-MUTATION GATE: PASS`

Do not mutate Azure before this marker.

---

# 9. Phase C — probe image readiness

Validate the retained public GHCR probe image is the intended minimal WP02 image and is safe/appropriate for the eight probes.

It must support:
- deterministic HTTP response;
- `/home` marker write/read;
- non-secret state/evidence;
- no database/provider/Twelve Data dependency.

If a new image revision is required, use the `sabsf` Docker handoff. Terra must generate exact commands and wait for evidence.

Required:

`AZURE F1 WP02 — MINIMAL PROBE IMAGE: PASS`

`AZURE F1 WP02 — PROBE IMAGE DISTRIBUTION: PASS`

---

# 10. Phase D — West Central US deployment

Create only the minimum authorized WP02 footprint in **West Central US**.

Expected architecture:
- one uniquely named owned resource group if required;
- one Linux App Service plan;
- SKU Free/F1;
- one custom-container Web App;
- persistent App Service storage enabled:
  `WEBSITES_ENABLE_APP_SERVICE_STORAGE=true`;
- public HTTPS;
- retained/free registry image reference;
- no unrelated resources.

Before issuing mutation commands Terra must state exact expected mutation counts.

After user execution, Terra must read back and validate:
- resource group ownership/name/location;
- plan location/SKU;
- Web App location/state;
- custom-container image configuration;
- persistent storage setting;
- HTTPS endpoint/configuration;
- absence of unauthorized dependencies.

Required:

`AZURE F1 WP02 — APP SERVICE F1 DEPLOYMENT: PASS`

---

# 11. Eight empirical probes

Each probe requires explicit evidence. Never batch away an evidence boundary where a mutation/restart/redeployment requires validation.

## Probe 1 — F1 availability
Prove the actual deployed plan is West Central US Free/F1.

`AZURE F1 WP02 PROBE 1 — F1 AVAILABILITY: PASS`

## Probe 2 — custom Docker
Prove App Service runs the intended custom probe image.

`AZURE F1 WP02 PROBE 2 — CUSTOM DOCKER: PASS`

## Probe 3 — public HTTPS
Prove the public HTTPS endpoint returns the deterministic expected response.

`AZURE F1 WP02 PROBE 3 — PUBLIC HTTPS: PASS`

## Probe 4 — persistent `/home`
Write a unique non-secret marker under `/home` and read the identical marker back.

`AZURE F1 WP02 PROBE 4 — PERSISTENT HOME: PASS`

## Probe 5 — App Service restart persistence
Use an authorized App Service restart, wait for healthy recovery, then prove the same `/home` marker persists.

`AZURE F1 WP02 PROBE 5 — APP RESTART PERSISTENCE: PASS`

## Probe 6 — container recycle persistence
Perform/observe a controlled container recycle distinguishable from Probe 5, then prove the same marker persists.

`AZURE F1 WP02 PROBE 6 — CONTAINER RECYCLE PERSISTENCE: PASS`

## Probe 7 — image redeployment persistence
Create/use a distinguishable image revision as required, redeploy it, then prove the pre-existing `/home` marker remains.

Docker build/tag/push uses the `sabsf` Docker handoff. Azure image configuration/redeployment uses the `sabsf` Azure CLI handoff.

`AZURE F1 WP02 PROBE 7 — IMAGE REDEPLOYMENT PERSISTENCE: PASS`

## Probe 8 — resource inventory
Enumerate every WP02-owned Azure resource and dependency. Prove no unauthorized resource/service was introduced and personal resources were untouched.

`AZURE F1 WP02 PROBE 8 — RESOURCE INVENTORY: PASS`

---

# 12. Resource disposition

Default: cleanup.

Retention for WP03 is permitted only if:
- published WP01 governance permits it;
- the exact retained resources are useful for WP03;
- recurring infrastructure cost remains `$0.00`;
- ownership/inventory is explicit;
- personal resources remain untouched.

Otherwise generate exact DELETE commands, STOP for execution evidence, then verify deletion via read-only inventory.

Clean temporary local Docker artifacts unless specifically retained as evidence. Account explicitly for GHCR artifacts.

Required:

`AZURE F1 WP02 — RESOURCE DISPOSITION: PASS`

---

# 13. Zero-cost evidence

Record evidence that the WP02 footprint introduced no known recurring infrastructure charge.

WP05 remains the final initiative-wide strict-$0 resource-envelope authority.

Required:

`AZURE F1 WP02 — ZERO-COST CONSTRAINT PRESERVED: PASS`

---

# 14. Mutation audit

Report historical and current-run mutations separately, then totals where useful.

Current-run exact counts must include:
- resource groups created/deleted/retained;
- App Service plans created/deleted/retained;
- Web Apps created/updated/restarted/redeployed/deleted/retained;
- configuration mutations;
- Docker images built/tagged/pushed/deleted;
- Docker containers created/run/deleted;
- registry artifacts created/retained/deleted;
- local temp files/directories;
- repository mutations;
- Git mutations;
- GitHub issue mutations;
- Project mutations;
- milestone mutations;
- Twelve Data requests;
- tags/releases.

User-executed `sabsf` operations count normally.

Required:

`AZURE F1 WP02 — MUTATION AUDIT: PASS`

---

# 15. WP02 acceptance

PASS only when:
- all eight empirical probes PASS;
- resource disposition PASS;
- zero-cost constraint PASS;
- mutation audit PASS;
- no personal Azure resource was mutated;
- evidence is sufficient and non-inferred.

Exact acceptance marker:

`AZURE F1 WP02 — APP SERVICE F1 EXECUTION PROBE: PASS`

Do not emit early.

Use NOT FEASIBLE only if empirical evidence proves the frozen candidate cannot satisfy a required condition without violating frozen constraints.

Use BLOCKED for authentication, quota, permissions, missing evidence, transient platform/tooling, or other remediable execution dependency.

---

# 16. Mandatory GitHub lifecycle after PASS

Only after the exact WP02 PASS marker:

1. close GitHub issue #253;
2. ensure Project #2 Status is Done;
3. if issue closure automation already sets Done, do not redundantly mutate Status;
4. count only explicit mutations;
5. verify #253 Closed/Done;
6. leave milestone #62 Open;
7. leave #254 Open/Todo.

Required:

`AZURE F1 WP02 — GITHUB LIFECYCLE: CLOSED/DONE`

---

# 17. WP03 handoff

Only after WP02 PASS and lifecycle completion:

`AZURE F1 WP03 — EXECUTION AUTHORITY: READY`

WP03 is issue #254:
**Persistent SQLite Filesystem, Locking & Journal Qualification**

Do not execute WP03 under this authority.

---

# 18. Model marker

`GPT-5.6 MODEL MAP: LUNA=CONTRACT/POLICY/GOVERNANCE | TERRA=IMPLEMENTATION/EXECUTION | SOL=SUPPORTING ANALYSIS`

---

# 19. Terminal outcomes

Success after PASS + #253 Closed/Done:

`AZURE F1 WP02 — EXECUTION AUTHORITY AMENDMENT COMPLETE`

Remediable blocker:

`AZURE F1 WP02 — EXECUTION AUTHORITY AMENDMENT BLOCKED`

Empirically proven candidate failure:

`AZURE F1 WP02 — APP SERVICE F1 EXECUTION PROBE: NOT FEASIBLE`
