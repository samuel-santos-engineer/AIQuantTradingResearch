Execute `init-1.11-wp05-f1-resource-envelope-strict-zero-cost-qualification-authority-terra.md` using **GPT-5.6 Terra**.

WP04 PASS/#255 Closed/Done. WP05=#256 Open/Todo. Target West Central US.

Primary objective: qualify the F1 resource envelope and support exactly:
`ACTUAL RECURRING INFRASTRUCTURE COST: $0.00`

Reuse WP02–WP04 evidence wherever sufficient; do not recreate resources just to repeat proven probes.

Authenticated Azure CLI (and any narrowly required Docker/WSL commands) execute in interactive `sabsf` PowerShell. For every batch give exact commands, classification, expected mutations, exit-code capture and sanitized evidence requirements, then STOP. Evaluate independently; never infer success. Never request payment-card data, full billing documents, credentials, tokens, or Azure CLI profile files.

Execute eight gates: F1 quota/free-tier evidence; mandatory dependency cost inventory; reference resource envelope; storage envelope; compute/runtime envelope; network/provider cost envelope; registry/deployment cost envelope; strict-$0 reconciliation; then disposition and mutation audit.

PASS is forbidden if mandatory recurring infrastructure cost is nonzero or materially unknown.

Only after `AZURE F1 WP05 — RESOURCE ENVELOPE & STRICT-ZERO-COST: PASS`, close #256 and ensure Project #2 Done without redundant direct Project mutation. Leave #62 Open and #257 Open/Todo. Emit lifecycle Closed/Done and `AZURE F1 WP06 — FEASIBILITY ACCEPTANCE AUTHORITY: READY`. WP06 is Luna-primary; do not execute it here. End with exact terminal.
