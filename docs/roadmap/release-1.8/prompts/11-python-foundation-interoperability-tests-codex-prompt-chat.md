Read `11-python-foundation-interoperability-tests-codex-prompt.md` completely and execute it as the sole authority for Release 1.8 WP11 — Python Foundation & Interoperability Tests, GitHub issue #221.
Convert the already-proven WP10 one-shot `.venv` JSON-over-stdio behaviors into permanent deterministic offline tests using only manifest-owned test projects/files, while preserving Application technology neutrality, Infrastructure ownership, and strict separation from WP08 scientific validation.
Permanently cover interpreter/entrypoint resolution, success round-trip, stdout/stderr discipline, versioning, malformed responses, non-zero exits, timeout, cancellation, failure translation, concurrent I/O, owned-process cleanup, and unrelated-Python-process safety without introducing a new Python test framework.
Do not redesign WP09/WP10, add ungoverned dependencies, mutate exact Python pins/schema, implement Release 1.9 ML behavior, execute WP12+, perform Git integration, or kill Python processes by name; use the Production Defect Rule if tests expose a real predecessor defect.
Run TST1–TST20, the new interoperability subset 3 consecutive times, WP08 regression, `pip check`, global cleanliness, and full .NET verification; close #221 Done only after zero-residue success and finish with the exact live #222 title in the authority's completion marker.
