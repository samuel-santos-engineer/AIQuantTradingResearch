Execute the corrective authority in `release-1.8-project-release-taxonomy-reconciliation-codex-prompt.md`.
Read Project #2 and its existing `Release` SINGLE_SELECT field completely, validate the live GraphQL schema, and capture every existing option ID/name/color/description before mutation.
If `1.8` is absent, use exactly one documented `updateProjectV2Field` mutation with the complete existing option set preserved by ID and append exactly one `1.8` option; stop on any ambiguity.
Do not create milestones, issues, Project items, dependencies, repository/Git changes, Python state, or Release 1.9 work; perform immediate read-back and prove existing option identities and assignments are unchanged.
Validate mutation accounting and repository cleanliness, then finish with the authority's exact completion markers without executing Release 1.8 GitHub Planning.
