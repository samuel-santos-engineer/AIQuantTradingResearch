Read `03-pipeline-identity-provenance-evidence-semantics-codex-prompt.md` completely and execute it as the sole Release 1.3 WP03 authority.
Using GPT-5.6 Sol, define only pipeline identity, provenance, lineage, equivalence, canonical representation, and semantic evidence while preserving the fixed WP02 topology and all Release 1.2 dataset identity semantics.
Create only the manifest-authorized WP03 semantic artifact; do not implement Application contracts, orchestration, persistence, DI, Worker behavior, tests, schema changes, durable run history, or Release 1.4 work.
Run every starting-state, canonical, architecture, security, whitespace, scope, and lifecycle gate; production/test/package/reference/schema deltas must remain zero.
Close #140 only if every WP03 acceptance gate passes, keep #141 Open/Backlog, and finish with the exact terminal marker required by the full prompt.
