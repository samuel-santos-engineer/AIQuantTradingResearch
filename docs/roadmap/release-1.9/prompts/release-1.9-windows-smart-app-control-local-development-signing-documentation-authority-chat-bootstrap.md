Execute `release-1.9-windows-smart-app-control-local-development-signing-documentation-authority-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Document the working Windows local-development Authenticode signing setup only. Create `docs/development/WINDOWS_SMART_APP_CONTROL_LOCAL_SIGNING.md`, then add at most two concise cross-references in the canonical developer/setup and Release 1.9/WP08 documentation.
Read the actual `eng/sec/create-self-signed-certificate.ps1`, `AIQuantTradingResearch.Application.csproj`, local-props import behavior, and `.gitignore` before writing. Document only factual implementation: Windows SDK install, local certificate bootstrap, opt-in Debug-only `AutoSignTestBinaries`, uncommitted `Directory.Build.local.props`, signature/App Control verification, troubleshooting, and repository security hygiene.
Do not modify source, tests, project files, scripts, `.gitignore`, packages, GitHub, or WP09. Explicitly separate the SAC/App Control environment remediation from the still-independent WP08 lifecycle defect. End with the authority's exact terminal marker.
