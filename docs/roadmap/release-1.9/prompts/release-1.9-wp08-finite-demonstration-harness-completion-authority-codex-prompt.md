# Release 1.9 — WP08 Finite-Demonstration Harness Completion Authority

## Model
Use **GPT-5.6 Terra**.

## Sole authority
This is a **narrow implementation/completion authority** for Release 1.9 WP08, canonical issue **#233**.

It exists only to complete the four remaining already-governed finite-demonstration acceptance surfaces:

1. independent Streamlit listener/readiness;
2. real WP05 → WP06 → WP07 chain observation;
3. restart sequence;
4. complete process/listener/handoff/database residue matrix.

The Worker cancellation/liveness implementation, reconciled P1→P2 seam, and Windows process-group signal helper are accepted predecessor WP08 work and are **not reopened**.

WP09 / #234 must remain Open / Backlog and unstarted.

---

# Binding WP08 authorities

Read completely:

1. `RELEASE_1.9_WP08_LIFECYCLE_BOUNDED_DEMONSTRATION_PROCESS_RESIDUE_CONTRACT_MANIFEST_PATH_AUTHORITY_DEFINITION.md`
2. `RELEASE_1.9_WP08_WORKER_LIFECYCLE_LIVENESS_TEST_SEAM_CANCELLATION_ADAPTER_CONTRACT_PATH_AUTHORITY_DEFINITION.md`
3. `RELEASE_1.9_WP08_WINDOWS_ISOLATED_PROCESS_GROUP_CONSOLE_SIGNAL_TEST_HELPER_AUTHORITY.md`
4. `RELEASE_1.9_WP08_LIVENESS_SEAM_BOUNDED_REFRESH_RECONCILIATION_CONTRACT_AMENDMENT.md`

Also read:
- current `WP08LifecycleDemonstrationTests.cs`;
- current `WindowsIsolatedProcessGroup.cs`;
- current accepted partial Worker files;
- accepted WP05/WP06/WP07 contracts and tests needed to understand the real chain;
- #233 acceptance criteria.

Authority 1 controls the missing demonstration and residue semantics.
Authority 4 controls P1/P2 progression.
Authorities 2/3 remain binding but their already-passing implementations must not be redesigned.

---

# Accepted predecessor state

Treat the following as the new accepted WP08 predecessor baseline:

## WP08 behavior
- P1 passes through normally.
- P2 is generated by existing Replay execution.
- P2 is observed within the fixed 8-second bound.
- Worker holds after P2.
- targeted `CTRL_BREAK_EVENT` is delivered to the owned process group.
- Worker exits gracefully with code 0.
- no forced kill is needed in the passing graceful-cancellation path.

## Focused WP08
- **4/4 passed**.

## Python
- WP05: **3/3**.
- WP06: **6/6**.
- WP07 semantic: **2/2**.
- WP07 presentation: **2/2**.
- Streamlit: **1.61.1**.
- `pip check`: clean.

## .NET
- Application: **125**.
- Infrastructure: **164**.
- Domain: **11**.
- Architecture: **13**.
- full: **313/313**.
- build: 0 warnings / 0 errors.

This 313/313 is the accepted predecessor regression baseline for this authority.

Do not revert to 309/309.

---

# Current lifecycle state

Expected:
- #233 Open / Backlog.
- #234 Open / Backlog.
- no GitHub mutations from the blocked pass.
- milestone #58 Open.

Verify read-only.

No GitHub mutation until all four missing surfaces and every final regression/scope gate pass.

---

# Hard preservation boundary

Do not reopen or redesign:

- `WorkerLifecycleCancellation.cs`;
- `Program.cs`;
- reconciled liveness behavior in `SimulatedLiveVisualizationExecution.cs`;
- `WindowsIsolatedProcessGroup.cs`;
- P1/P2 newer-publication semantics;
- 8-second bound;
- targeted CTRL_BREAK mechanism;
- Windows command-line quoting;
- Replay;
- WP05 transport/parser;
- WP06 frame semantics;
- WP07 semantic/presentation contracts.

If a missing demonstration surface requires changing any of those, STOP and report the exact missing authority.

---

# Mutation scope

Primary authorized path:

`tests/AIQuantTradingResearch.Infrastructure.Tests/WP08LifecycleDemonstrationTests.cs`

Use the exact binding path from the accepted WP08 path authority if it differs.

The existing:

`tests/AIQuantTradingResearch.Infrastructure.Tests/WindowsIsolatedProcessGroup.cs`

may be **used**, but not modified unless an actual helper defect is proven. If a helper defect is found, STOP; this authority does not authorize redesign.

No production mutations are expected or authorized.

No new helper/test path unless authority 1 explicitly already authorizes it. Otherwise STOP.

---

# Objective

Complete the existing focused WP08 harness so one finite, deterministic, owned local demonstration proves:

`Worker → atomic JSON handoff → WP05 parser → WP06 VisualizationFrame → WP07 presentation projection`

while also proving:

- Streamlit is independently launched;
- Streamlit owns its listener;
- Worker owns no listener;
- bounded refresh remains valid;
- graceful Worker cancellation remains valid;
- Worker restart works;
- Streamlit shutdown works;
- all harness-owned process/listener/handoff/database residue satisfies authority 1.

---

# Phase 0 — Extract exact remaining contract

Before mutation, extract from authority 1:

- total finite demonstration max duration;
- Streamlit startup/readiness timeout;
- exact readiness endpoint/condition;
- loopback host requirements;
- port allocation rule;
- Worker/Streamlit launch ordering;
- real-chain observation rule;
- restart ordering;
- Worker restart timeout;
- Streamlit shutdown method/timeout;
- final shutdown ordering;
- process residue definition;
- listener residue definition;
- canonical handoff final-state rule;
- temp handoff glob/pattern;
- DB location/ownership;
- DB/WAL/SHM/journal residue rule;
- evidence/log persistence rule;
- cleanup fallback behavior.

Do not infer missing values.

If any material acceptance value is undefined, STOP.

---

# Phase 1 — Read-only repository inspection

Inspect:

- current WP08 lifecycle test;
- existing process launch patterns;
- existing Streamlit test/smoke launch patterns;
- current Python entry point;
- parser/frame/presentation pure functions;
- current handoff path configuration;
- temporary DB configuration;
- existing listener/port utilities;
- current test project dependencies.

Prefer existing BCL/repository facilities.

No package additions.

---

# Phase 2 — Pre-mutation baseline gate

Run:

- focused WP08 4/4;
- build;
- full .NET 313/313;
- WP05 Python 3/3;
- WP06 Python 6/6;
- WP07 semantic 2/2;
- WP07 presentation 2/2;
- Streamlit import/version;
- `pip check`.

Also prove no pre-existing harness-owned process/listener/temp runtime residue.

If baseline fails, STOP.

---

# Phase 3 — Harness-owned runtime allocation

Within the existing WP08 test path, create deterministic per-test/session ownership for:

- runtime directory;
- canonical handoff path;
- temporary DB path;
- loopback Streamlit port;
- Worker process;
- Streamlit process.

Every owned resource must have an explicit cleanup owner.

Do not touch developer/default runtime artifacts.

---

# Phase 4 — Streamlit process launch

Launch the real accepted Streamlit entry point as an **independent peer process**.

Requirements:

- Worker does not launch Streamlit.
- Streamlit does not launch Worker.
- bind loopback only as required by authority 1.
- use one harness-owned free port.
- no browser automation unless explicitly required.
- no production code changes.

Use existing Python environment/interpreter conventions.

---

# Phase 5 — Streamlit readiness

Prove readiness using the exact authority-1 condition.

Readiness must establish:

- Streamlit process alive;
- expected loopback listener active;
- readiness response/condition succeeds;
- listener belongs to the harness-owned Streamlit process or its governed owned process tree;
- Worker is not the listener owner.

Sleep-only readiness is forbidden.

Timeout:
- fail test;
- clean only owned resources.

---

# Phase 6 — Listener ownership

Record exact:

- host;
- port;
- listener PID/owner;
- Worker PID;
- Streamlit PID.

Prove:

`listener owner != Worker`

and listener is within the owned Streamlit process identity/tree as defined by the contract.

Do not rely only on “port open”.

No global listener cleanup.

---

# Phase 7 — Worker launch and P1/P2 preservation

Use the accepted Windows helper.

Launch real Worker with:

`--wp08-test-liveness`

and isolated harness configuration.

Preserve accepted proof:

- P1 valid;
- P1 pass-through;
- P2 genuinely newer;
- P2 within 8 seconds;
- Worker holds after P2.

Do not alter this sequence.

---

# Phase 8 — Real WP05 parser observation

From the **real Worker-produced canonical handoff**, invoke/use the actual WP05 parser surface exactly as governed.

Prove:
- canonical envelope parses;
- revision/state/metadata are real;
- no fabricated replacement fixture;
- no SQLite/provider reconstruction;
- no cache/transport semantic changes.

The test may call pure Python functionality through the existing governed process/module mechanism only if already authorized by authority 1.

Do not create a new transport.

---

# Phase 9 — Real WP06 frame observation

Using the parsed real handoff:

- project through the actual WP06 `VisualizationFrame` projection;
- assert deterministic factual frame fields required by the existing WP06 contract;
- prove data comes from P1/P2 Worker handoff.

Do not reimplement WP06 formulas in .NET.

If cross-language observation is required, use the exact accepted Python invocation mechanism.

---

# Phase 10 — Real WP07 presentation observation

Using the same real chain:

- invoke actual `project_wp07_presentation_sections(...)` or exact governed WP07 surface;
- assert required sections/labels/order/state;
- assert canonical idempotency/data-quality facts;
- preserve transport-warning separation.

This must be actual WP07 code, not a duplicated .NET projection.

---

# Phase 11 — Chain identity proof

Tie the chain together.

Prove that the handoff revision/snapshot/tick used by:

- WP05 parser;
- WP06 frame;
- WP07 projection

corresponds to the same real Worker-produced publication selected by the demonstration.

Do not accept unrelated fixture output.

---

# Phase 12 — Streamlit + handoff coexistence

While Streamlit is alive:

- Worker publication remains readable;
- Streamlit listener remains ready;
- no process owns the other's lifecycle;
- no handoff deletion by Streamlit;
- no Worker listener appears.

Do not require UI screenshot/browser proof unless authority 1 does.

---

# Phase 13 — Graceful Worker cancellation preservation

After required P2/chain observations:

- verify Worker alive;
- send accepted targeted CTRL_BREAK;
- API success;
- Worker exits within accepted timeout;
- exit code 0;
- no forced kill in passing path.

Re-use accepted helper; do not modify signal mechanism.

---

# Phase 14 — Session A post-cancellation state

Immediately after Worker A exits, record:

- canonical handoff existence/state;
- temp handoff siblings;
- DB and sidecars;
- Streamlit process/listener state.

Apply authority-1 rules exactly.

Do not prematurely clean artifacts needed to prove restart cleanup semantics.

---

# Phase 15 — Restart sequence

Execute exact authority-1 restart using the same isolated runtime ownership boundary.

Worker B must:

- start within restart bound;
- exercise startup cleanup as defined;
- not inherit liveness seam state from A;
- produce a valid new canonical publication;
- satisfy required revision/session behavior;
- remain compatible with still-independent Streamlit if contract requires Streamlit to remain alive.

Do not change Replay.

---

# Phase 16 — Restart cleanup proof

Prove the exact startup-cleanup requirement:

- prior canonical handoff handling;
- stale temp sibling handling;
- DB behavior;
- no stale process/listener ownership.

Distinguish:
- allowed persistent canonical state;
- required startup deletion;
- newly published B state.

Use exact authority-1 semantics.

---

# Phase 17 — Worker B termination

Terminate/cancel Worker B using the exact accepted lifecycle contract required by authority 1.

If graceful cancellation proof is required again:
- use targeted CTRL_BREAK.

If B may naturally complete under the contract:
- follow that exact rule.

No broad kill.

---

# Phase 18 — Streamlit shutdown

After the contract-fixed final observation/order:

- request/perform the least-forceful authorized Streamlit termination;
- wait within timeout;
- use owned forced fallback only if permitted;
- record whether fallback was needed;
- prove listener released.

If authority 1 requires graceful-only Streamlit shutdown, fallback means failure.

---

# Phase 19 — Process residue matrix

At final cleanup prove exact matrix:

| Resource | Expected |
|---|---|
| Worker A | absent |
| Worker B | absent |
| Streamlit | absent |
| owned child processes | absent |
| unrelated processes | untouched |

Use PIDs/owned handles, not process-name assumptions.

---

# Phase 20 — Listener residue matrix

Prove:

- harness Streamlit port no longer listening;
- no Worker listener was ever introduced;
- no other harness-owned listener remains;
- unrelated listeners untouched.

Record port and ownership evidence.

---

# Phase 21 — Handoff residue matrix

Check:

- canonical handoff exact final-state rule;
- zero forbidden temporary handoff siblings;
- no abandoned atomic-write temp artifact;
- Streamlit did not delete canonical handoff.

Use exact glob/pattern from authority 1.

---

# Phase 22 — Database residue matrix

Check exact harness-owned:

- main DB;
- `-wal`;
- `-shm`;
- journal;
- any other governed sidecar.

Apply authority-1 expected final-state rule.

Do not inspect/delete unrelated developer DB.

---

# Phase 23 — Evidence/log residue

Follow authority 1 exactly.

If evidence is test output only:
- persistent evidence artifacts zero.

If a bounded evidence path is already authorized:
- use only that path.

No new evidence directory.

---

# Phase 24 — Finite duration

Measure the complete demonstration using authority-1 timer boundaries.

Report:
- start;
- finish;
- elapsed;
- maximum.

If elapsed exceeds max, FAIL.

Do not increase timeout.

---

# Phase 25 — Focused WP08 suite

Run the complete focused WP08 test surface.

The previous 4/4 must remain green.

Report:
- new exact count;
- exact added tests/assertion scenarios;
- duration;
- 0 failed;
- skipped count.

Any new .NET test count must be fully attributable to this authorized test path.

---

# Phase 26 — Python predecessor regression

Run:

- WP05 3/3;
- WP06 6/6;
- WP07 semantic 2/2;
- WP07 presentation 2/2;
- compile/import;
- Streamlit 1.61.1;
- `pip check`.

No residue afterward.

---

# Phase 27 — .NET regression

Run:

- Application;
- Infrastructure;
- Domain;
- Architecture;
- build;
- full solution.

Accepted predecessor baseline entering this authority:

**313/313**

Explain any new authorized WP08 test delta exactly.

Require:
- 0 failed;
- 0 skipped unless pre-existing governance explicitly permits;
- build 0 warnings / 0 errors.

---

# Phase 28 — Static scope audit

Prove zero changes to:

- Worker cancellation adapter;
- P1/P2 seam;
- Windows helper;
- Replay;
- WP05 production/parser semantics;
- WP06 production semantics;
- WP07 production semantics;
- schema;
- persistence;
- provider;
- package pins;
- production process supervision;
- custom IPC;
- sockets/listeners added to Worker;
- Streamlit production supervision;
- WP09.

List every changed path.

Expected: only the WP08 lifecycle demonstration test path.

---

# Phase 29 — Preservation audit

Prove accepted partial WP08 remains intact:

- 4/4 predecessor focused behaviors preserved;
- graceful CTRL_BREAK still exits 0;
- P2 still within 8 sec;
- Windows quoting/helper unchanged.

Unrelated local work untouched.

---

# Phase 30 — #233 final acceptance matrix

Before GitHub mutation produce:

`#233 requirement → binding authority → test/demo evidence → residue evidence`

Explicit rows for:

- restart;
- cancellation;
- bounded refresh;
- process ownership;
- listener ownership;
- temporary handoff residue;
- database residue;
- finite demonstration;
- real WP05→WP06→WP07 chain;
- independent Streamlit.

Every row PASS.

Otherwise:
- #233 remains Open / Backlog;
- GitHub mutations zero.

---

# Phase 31 — GitHub Project item identification

Only after every technical gate passes.

Identify exactly one Project #2 item for #233.

Use robust typed lookup and exhaustive pagination.

Resolve:
- item node ID;
- Status field ID;
- Done option ID;
- Release;
- Priority;
- Area.

If ambiguous, BLOCK with zero GitHub mutation.

No item creation/deletion.

---

# Phase 32 — GitHub lifecycle completion

Only after identity proof:

1. set #233 Project Status → Done;
2. read back Done;
3. verify Release/Priority/Area unchanged;
4. close #233;
5. read back Closed;
6. verify #234 Open / Backlog;
7. verify milestone #58 state/counts.

Do not mutate #234.
Do not start WP09.

---

# Phase 33 — Final read-back

After lifecycle mutation, no repository changes.

Verify:
- #233 Closed / Done;
- #234 Open / Backlog;
- no Worker residue;
- no Streamlit residue;
- no harness listener;
- handoff final state correct;
- DB final state correct.

---

# Completion gate

WP08 completes only if all are true:

1. independent Streamlit launch/readiness proven;
2. Streamlit listener ownership proven;
3. Worker owns no listener;
4. real WP05 parser observation proven;
5. real WP06 frame observation proven;
6. real WP07 projection proven;
7. same Worker publication identity tied through chain;
8. P1/P2/8-second proof remains green;
9. graceful CTRL_BREAK exit 0 remains green;
10. restart passes;
11. Streamlit shutdown passes;
12. process residue passes;
13. listener residue passes;
14. handoff residue passes;
15. DB residue passes;
16. finite duration passes;
17. Python predecessors pass;
18. .NET/build pass from 313 baseline plus only authorized delta;
19. scope/preservation pass;
20. #233 Project Done;
21. #233 Closed;
22. #234 Open / Backlog;
23. WP09 unstarted.

---

# Required completion report

## Accepted predecessor
4/4 WP08, 313/313 .NET, Python baselines.

## Changed paths
Exact list.

## Streamlit
Launch/readiness/listener ownership.

## Real chain
Worker → WP05 → WP06 → WP07 evidence.

## Bounded refresh/cancellation
Preserved P1/P2 and CTRL_BREAK evidence.

## Restart
A → cleanup → B proof.

## Shutdown
Worker/Streamlit behavior.

## Residue matrix
Processes/listeners/handoff/DB/evidence.

## Duration
Actual vs maximum.

## Focused WP08
Exact count/result.

## Python
Exact results.

## .NET
Exact suite totals/build.

## Scope/preservation
Forbidden mutations zero.

## #233 mapping
Requirement → proof.

## GitHub
#233 Closed / Done; #234 unchanged.

## Milestone
State and canonical/raw counts.

## Next eligible work package
State exact next canonical WP.

---

# Mutation statements

On successful lifecycle completion:

`WP08 LIFECYCLE GITHUB MUTATIONS: #233 PROJECT STATUS → DONE; #233 ISSUE → CLOSED; ALL OTHER GITHUB MUTATIONS ZERO`

Report repository mutations exactly.

---

# Stop conditions

Stop if:

- Streamlit readiness/listener ownership cannot be proven under existing authority;
- real WP05→WP06→WP07 observation requires a new transport/helper path not already authorized;
- restart requires production changes;
- residue semantics are undefined;
- Worker seam/helper must be changed;
- Replay/WP05/WP06/WP07 production must change;
- package addition is required;
- finite duration cannot be met;
- regression fails;
- Project item identity is ambiguous;
- WP09 boundary would be crossed.

Preserve all valid partial WP08 work and keep #233 Open / Backlog.

---

# Terminal markers

Success:

`RELEASE 1.9 WP08 FINITE-DEMONSTRATION HARNESS COMPLETION COMPLETE`

Blocked:

`RELEASE 1.9 WP08 FINITE-DEMONSTRATION HARNESS COMPLETION BLOCKED`

Do not emit COMPLETE unless all four previously missing demonstration surfaces are proven, all final regression/scope gates pass, and #233 is authoritatively Closed / Done.
