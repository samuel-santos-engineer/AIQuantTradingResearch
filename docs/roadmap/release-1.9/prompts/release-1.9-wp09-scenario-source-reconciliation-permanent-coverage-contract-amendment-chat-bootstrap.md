Execute `release-1.9-wp09-scenario-source-reconciliation-permanent-coverage-contract-amendment-codex-prompt.md` as the sole authority using GPT-5.6 Luna.

This is documentation-only. Read the binding WP09 contract plus the exact predecessor Replay, pipeline/composition, read-model, WP05, WP06, WP07, and WP08 contracts. Reconcile the proven source mismatch without changing frozen production behavior.

Keep PI-READY and PI-WARMUP on the real Replay→pipeline→read-model→handoff chain. Assign PI-EMPTY and PI-FAILED to the deepest existing canonical historical-composition/pipeline/read-model boundaries that actually own those states, naming the exact repository symbols after inspection. Preserve all four permanent scenarios, downstream assertions, architecture rules, existing WP09 path authority, +12 .NET / +4 Python / +16 total, 327 baseline, and 339 expected .NET total.

Modify only the binding WP09 documentation artifact. No tests, production, Python, Replay, Worker, packages, schema, GitHub, or WP10+ mutations. If no existing governed Empty/Failed composition boundary exists, stop rather than inventing semantics. On success state that the existing consolidated GPT-5.6 Terra implementation authority may resume. End with the exact terminal marker.
