Read `release-roadmap-malformed-milestone-57-correction-codex-prompt.md` completely and execute it as the sole corrective authority for the blocked roadmap reconciliation.
First prove #57 still exactly matches the malformed empty milestone (`System.Collections.Specialized.OrderedDictionary.title` / `.description`, OPEN, 0/0, no dependencies), then delete #57 only; preserve the already-successful Project Release taxonomy through 2.3 and milestone #50→2.1 / #51→2.2 mappings.
Resume idempotently by creating the four intended empty placeholders one at a time with immediate read-back: 1.9 Real-Time Financial Data Visualization, 1.10 OpenTelemetry & Pipeline Observability, 2.0 Lightweight Machine Learning Evaluation, and 2.3 Backtesting; create no issues/WPs.
Then resume the original roadmap reconciliation from documentation onward on `docs/roadmap-release-sequencing`, commit all governed Markdown including both authority pairs if repository-resident, push the branch, open/validate the PR, and merge only under the original authority's merge gate; never push directly to `main`.
Run C57-1–C57-20 plus the resumed RR/engineering gates, capture the resulting `main` merge SHA, leave Release 1.9 undefined/unimplemented, and finish with the authority's exact success or blocked markers.
