Execute `release-1.9-wp08-governed-python-presentation-chain-invocation-seam-path-authority-amendment-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Define exactly one bounded WP08 test/demo-only Python seam that accepts the harness-owned real Worker handoff path, delegates to the existing WP05 parser, existing WP06 VisualizationFrame projection, and existing WP07 presentation-section projection, then emits one deterministic JSON evidence document on stdout and exits.
Amend only the exact Python probe path and `WP08LifecycleDemonstrationTests.cs` invocation/assertion authority, plus one dedicated WP08 Python probe test path only if repository governance requires it. Preserve WP08 4/4, .NET 313/313, Worker/Windows helper, WP05/WP06/WP07 semantics, Streamlit, and the Release 1.8 JSON-over-stdio endpoint. No generic bridge, arbitrary module execution, new network/IPC transport, duplicated parser/projection, package, GitHub, or WP09 mutation. End with the authority's exact terminal marker.
