# Release 1.9 — WP08 Finite-Demonstration Consolidated Completion — Fresh Authority

## Model
Use **GPT-5.6 Terra**.

## Sole authority
This prompt is the fresh consolidated implementation/completion authority for Release 1.9 **WP08 — canonical issue #233**.

It exists to complete the remaining finite-demonstration acceptance work after the governed Python presentation-chain invocation seam was defined.

WP08 may be closed only if every finite-demonstration, cross-language observation, restart, residue, regression, preservation, scope, and GitHub lifecycle gate passes.

**WP09 / #234 must remain Open / Backlog and unstarted.**

# Binding WP08 authorities

Read completely and treat as binding:

1. `docs/roadmap/release-1.9/RELEASE_1.9_WP08_LIFECYCLE_BOUNDED_DEMONSTRATION_PROCESS_RESIDUE_CONTRACT_MANIFEST_PATH_AUTHORITY_DEFINITION.md`
2. `docs/roadmap/release-1.9/RELEASE_1.9_WP08_WORKER_LIFECYCLE_LIVENESS_TEST_SEAM_CANCELLATION_ADAPTER_CONTRACT_PATH_AUTHORITY_DEFINITION.md`
3. `docs/roadmap/release-1.9/RELEASE_1.9_WP08_WINDOWS_ISOLATED_PROCESS_GROUP_CONSOLE_SIGNAL_TEST_HELPER_AUTHORITY.md`
4. `docs/roadmap/release-1.9/RELEASE_1.9_WP08_LIVENESS_SEAM_BOUNDED_REFRESH_RECONCILIATION_CONTRACT_AMENDMENT.md`
5. `docs/roadmap/release-1.9/RELEASE_1.9_WP08_GOVERNED_PYTHON_PRESENTATION_CHAIN_INVOCATION_SEAM_PATH_AUTHORITY_AMENDMENT.md`

Also preserve accepted Release 1.9, WP05, WP06, WP07, reconciliation, and current partial WP08 work.

Authority precedence:
- Artifact 4 supersedes only the contradictory seam progression in artifact 2.
- Artifact 5 adds only the bounded test/demo Python invocation seam and corresponding path authority.
- Artifact 1 controls finite lifecycle/demo/residue timing and acceptance.
- Artifact 2 controls cancellation adapter and test-liveness semantics except where amended by artifact 4.
- Artifact 3 controls Windows process-group launch and targeted CTRL_BREAK.
- Existing predecessor contracts govern everything else.

If any material conflict exists, STOP.

# Accepted predecessor state

Treat the following as accepted and do not reopen it.

## WP08 focused behavior
- P1 passes through normally.
- P2 is produced by existing Replay execution.
- P2 is genuinely newer under existing semantics.
- P2 is observed within the fixed **8-second** bound.
- Worker holds after P2.
- targeted `CTRL_BREAK_EVENT` reaches the owned Worker group.
- Worker exits gracefully with code `0`.
- no forced kill is required in the passing graceful-cancellation path.
- focused WP08 tests: **4/4 passed**.

## Existing partial WP08 paths
Production:
- `src/AIQuantTradingResearch.Worker/WorkerLifecycleCancellation.cs`
- `src/AIQuantTradingResearch.Worker/Program.cs`
- `src/AIQuantTradingResearch.Worker/SimulatedLiveVisualizationExecution.cs`

Tests:
- `tests/AIQuantTradingResearch.Infrastructure.Tests/WindowsIsolatedProcessGroup.cs`
- `tests/AIQuantTradingResearch.Infrastructure.Tests/WP08LifecycleDemonstrationTests.cs`

## Python predecessor
- WP05: **3/3**
- WP06: **6/6**
- WP07 semantic: **2/2**
- WP07 presentation: **2/2**
- Streamlit: **1.61.1**
- `pip check`: clean

## .NET predecessor
- Application: **125**
- Infrastructure: **164**
- Domain: **11**
- Architecture: **13**
- full: **313/313**
- build: 0 warnings / 0 errors

Use **313/313** as the accepted .NET predecessor baseline.

# Lifecycle state

Expected:
- #232 Closed / Done
- #233 Open / Backlog
- #234 Open / Backlog
- milestone #58 Open
- no GitHub mutations from the blocked finite-demonstration pass
- WP09 unstarted

Verify read-only.

# Objective

Complete the remaining WP08 finite-demonstration work by implementing only the newly authorized Python probe seam and the missing harness assertions/orchestration needed to prove:

1. independent Streamlit listener/readiness;
2. real Worker handoff → existing WP05 parser → existing WP06 frame → existing WP07 presentation projection;
3. restart;
4. complete process/listener/handoff/database residue matrix;
5. finite total duration;
6. all predecessor regressions;
7. final #233 lifecycle completion.

Do not reopen the already-passing Worker seam or Windows signal helper.

# Phase 0 — Extract exact contracts

Before mutation, extract exact values/symbols from all five authorities, including all timing, readiness, residue, Python probe command, exact WP05/WP06/WP07 symbols, output schema, identity fields, exit codes, stderr rules, 2-second timeout, and exact path authority.

If any required value remains undefined, STOP.

# Phase 1 — Repository/GitHub verification

Read-only verify branch/HEAD/origin/ahead-behind, staged/unstaged/untracked paths, current diffs of accepted partial WP08 files, authorized Python probe presence/absence, #233 state and Project item, #232 Closed / Done, #234 Open / Backlog, milestone #58, and no WP09 implementation.

Do not require a clean worktree. Preserve accepted partial WP08 and unrelated local work.

# Phase 2 — Pre-mutation baseline gate

Run focused WP08 4/4, build, full .NET 313/313, WP05 3/3, WP06 6/6, WP07 semantic 2/2, WP07 presentation 2/2, compile/import, Streamlit 1.61.1, and `pip check`.

Also prove no pre-existing harness-owned Worker, Streamlit, listener, temp handoff, or temp DB/sidecar residue.

If baseline fails, STOP.

# Phase 3 — Path/symbol hard gate

New implementation is limited to the exact Python probe path authorized by artifact 5 and `tests/AIQuantTradingResearch.Infrastructure.Tests/WP08LifecycleDemonstrationTests.cs`.

Existing Windows helper may be used but not modified unless a helper defect is proven; if proven, STOP.

No new production paths, helper paths, or WP09 paths.

# Phase 4 — Implement the governed Python probe

Create only the exact authorized Python probe.

Requirements:
- acceptance/demo-only;
- one required absolute harness-owned handoff path;
- no arbitrary module/function argument;
- no generic bridge;
- no network;
- no DB/provider reconstruction;
- no persistent output file;
- delegate only to existing WP05/WP06/WP07 code.

# Phase 5 — WP05 delegation

Call the exact accepted WP05 parser symbol from artifact 5.

No duplicate JSON schema logic, manual parser substitute, cache/retry redesign, or semantic change.

If parser cannot be imported as accepted, STOP.

# Phase 6 — WP06 delegation

Call the exact accepted WP06 frame projection symbol.

No duplicated price/time/window/latest logic, alternate frame, or semantic change.

If importability requires production refactor, STOP.

# Phase 7 — WP07 delegation

Call `project_wp07_presentation_sections(...)` or the exact binding symbol.

Preserve five-section structure/order, labels/formatting, canonical statuses, and transport-warning separation.

No duplicate formatter.

# Phase 8 — Probe deterministic evidence JSON

Emit exactly one UTF-8 JSON document on stdout using the exact schema fixed by artifact 5.

Diagnostics go to stderr only.

No JSONL and no output file.

# Phase 9 — Probe identity correlation

Use only existing identifiers fixed by artifact 5 to prove Worker handoff == WP05 source == WP06 frame source == WP07 projection source.

Do not invent a correlation ID.

# Phase 10 — Probe exit/timeout

Use exact exit domain from artifact 5.

Probe runs once and exits. Harness timeout is **2 seconds** exactly unless artifact 5 says otherwise.

No polling.

# Phase 11 — Probe residue/security

No process residue, listener, temp output, DB mutation, provider/network access, eval/exec, dynamic module argument, or shell=True.

# Phase 12 — Extend WP08 lifecycle harness

Modify only the existing WP08 lifecycle test path.

Add bounded invocation for the exact probe: accepted Python interpreter, exact probe path, exact handoff arg, stdout/stderr capture, 2-second timeout, deterministic JSON parse, and process disposal.

No generic Python execution API.

# Phase 13 — Harness-owned runtime allocation

Use authority-1 rules for isolated runtime dir, canonical handoff, isolated temp DB, harness-owned loopback Streamlit port, Worker A/B, Streamlit, and probe process.

Track all ownership.

# Phase 14 — Streamlit launch

Launch real existing Streamlit entry point independently, loopback-only on harness-owned port, with existing Python environment.

Worker does not launch Streamlit and Streamlit does not launch Worker.

No production code change.

# Phase 15 — Streamlit readiness

Prove exact authority-1 readiness: process alive, expected listener ready within timeout, listener owner belongs to owned Streamlit identity/tree, and Worker is not the listener owner.

Sleep-only readiness forbidden.

# Phase 16 — Listener ownership

Record host, port, Worker PID, Streamlit PID, and listener owner. Prove listener owner is not Worker and is owned by Streamlit under the binding rule.

# Phase 17 — Worker P1/P2 preservation

Launch real Worker with accepted Windows helper and `--wp08-test-liveness`.

Preserve P1 valid, P1 pass-through, P2 genuinely newer, P2 within 8 seconds, post-P2 hold.

Do not modify seam/helper.

# Phase 18 — Probe real P2 handoff

Invoke the governed probe against the qualifying real Worker-produced P2 canonical handoff.

Require exit 0 and valid deterministic JSON within 2 seconds.

No fabricated handoff fixture.

# Phase 19 — Assert real WP05 observation

Assert only the WP05 facts required by artifact 5 and #233.

# Phase 20 — Assert real WP06 observation

Assert only the governed WP06 frame evidence and same publication identity.

# Phase 21 — Assert real WP07 observation

Assert exact WP07 presentation evidence and same publication identity.

# Phase 22 — Chain identity proof

Prove the same real Worker P2 publication flows through WP05, WP06, and WP07.

If identity is ambiguous, FAIL.

# Phase 23 — Streamlit coexistence

While Streamlit remains alive, prove canonical handoff remains readable, listener remains ready, Streamlit does not delete handoff, Worker owns no listener, and neither process owns the other's lifecycle.

# Phase 24 — Graceful cancellation preservation

After P2/probe/chain evidence, send targeted CTRL_BREAK. Require API success, graceful exit within timeout, code 0, no forced kill, and no Worker residue.

# Phase 25 — Session A residue snapshot

After Worker A exits but before restart cleanup, record canonical handoff, temp siblings, DB/sidecars, and Streamlit process/listener state exactly as authority 1 requires.

# Phase 26 — Restart sequence

Launch Worker B with the same isolated runtime. Prove startup within bound, prior-session cleanup, seam state reset, valid new publication, accepted revision/session behavior, and independent Streamlit behavior if required.

No Replay change.

# Phase 27 — Worker B completion

Terminate/cancel B exactly as authority 1 requires. No broad kill.

# Phase 28 — Streamlit shutdown

Use exact authority-1 shutdown method/order, bounded wait, permitted fallback only, listener release, and no owned child residue.

# Phase 29 — Process residue matrix

Final state: Worker A absent, Worker B absent, Streamlit absent, probe absent, owned child processes absent, unrelated processes untouched.

# Phase 30 — Listener residue matrix

Harness Streamlit port no longer listening, no Worker listener introduced, no other owned listener remains, unrelated listeners untouched.

# Phase 31 — Handoff residue matrix

Apply exact authority-1 final-state rules for canonical handoff and temp siblings. Streamlit never deletes canonical handoff.

# Phase 32 — DB residue matrix

Apply exact authority-1 rules for DB, WAL, SHM, journal, and other governed sidecars. Only harness-owned DB artifacts.

# Phase 33 — Evidence/log residue

Follow authority 1 exactly. No new evidence directory.

# Phase 34 — Finite total duration

Measure complete demonstration under exact authority-1 maximum. Probe's 2-second timeout counts toward total duration.

Exceeding max = FAIL.

# Phase 35 — Focused WP08 suite

Run full WP08 focused tests. Accepted predecessor 4/4 must remain green.

Report final count, authorized added tests/assertions, duration, failed/skipped.

# Phase 36 — Python predecessor regression

Run WP05 3/3, WP06 6/6, WP07 semantic 2/2, WP07 presentation 2/2, compile/import, Streamlit 1.61.1, and `pip check`.

No residue afterward.

# Phase 37 — .NET governed regression

Run Application, Infrastructure, Domain, Architecture, build, and full solution.

Accepted baseline entering this authority: **313/313**.

Explain exact authorized test delta. Require 0 failed and build 0 warnings / 0 errors.

# Phase 38 — Static scope audit

Prove zero unauthorized Worker cancellation/seam redesign, Windows helper redesign, Replay change, WP05/WP06/WP07 semantic change, Release 1.8 endpoint expansion, generic Python bridge, arbitrary module invocation, persistent Python service, network/IPC transport, provider/SQLite reconstruction, package additions, production supervisor, or WP09 work.

Expected changed paths: only the exact Python probe path and existing WP08 lifecycle test path.

# Phase 39 — Preservation audit

Prove existing 4/4 behavior, P2 within 8 seconds, graceful CTRL_BREAK exit 0, unchanged Windows helper, unchanged partial Worker files, and unrelated local work preserved.

# Phase 40 — #233 acceptance matrix

Before GitHub mutation, map every #233 requirement to binding authority, test/demo evidence, and residue evidence.

Explicit rows: bounded refresh, cancellation, restart, process ownership, listener ownership, real WP05 observation, real WP06 observation, real WP07 observation, same-publication correlation, handoff residue, DB residue, finite demo, independent Streamlit.

Every row must PASS.

# Phase 41 — GitHub Project item identification

Only after all technical acceptance. Identify exactly one Project #2 item for #233 with typed variables, exhaustive pagination, and authoritative issue content. Resolve item ID, Status field ID, Done option ID, Release, Priority, and Area.

If ambiguous, BLOCK with zero GitHub mutation.

# Phase 42 — GitHub lifecycle completion

After identity proof:
1. set #233 Project Status → Done;
2. read back Done;
3. verify metadata unchanged;
4. close #233;
5. read back Closed;
6. verify #234 Open / Backlog;
7. verify milestone #58 state/counts.

Do not mutate #234 or start WP09.

# Phase 43 — Final read-back/residue

After GitHub mutation, no repository changes.

Verify #233 Closed / Done, #234 Open / Backlog, no Worker A/B, no Streamlit, no probe, no listener, handoff final state correct, DB final state correct.

# Completion gate

WP08 completes only if all are true:
1. governed Python probe implemented exactly;
2. real P2 handoff parsed by actual WP05;
3. actual WP06 frame projection runs;
4. actual WP07 presentation projection runs;
5. same publication identity correlates end-to-end;
6. independent Streamlit readiness/listener ownership proven;
7. bounded refresh still passes;
8. graceful CTRL_BREAK still exits 0 without forced kill;
9. restart passes;
10. process residue passes;
11. listener residue passes;
12. handoff residue passes;
13. DB residue passes;
14. finite total duration passes;
15. Python predecessors pass;
16. .NET/build pass from 313 baseline plus only authorized test delta;
17. scope/preservation pass;
18. #233 Project Done;
19. #233 Closed;
20. #234 Open / Backlog;
21. WP09 unstarted.

# Required completion report

Report:
- all five binding WP08 authorities;
- accepted predecessor 4/4 and 313/313;
- exact changed paths;
- Python probe path/command/delegated symbols/timeout/output schema;
- Streamlit readiness/listener ownership;
- real Worker P2 → WP05 → WP06 → WP07 correlation;
- bounded refresh/cancellation;
- restart/shutdown;
- process/listener/handoff/DB/evidence residue;
- actual vs max duration;
- focused WP08 count;
- Python regressions;
- .NET suite totals/build;
- scope/preservation;
- #233 requirement mapping;
- GitHub read-back;
- milestone state/counts;
- exact next eligible WP.

# Mutation statement

If lifecycle completion succeeds:

`WP08 LIFECYCLE GITHUB MUTATIONS: #233 PROJECT STATUS → DONE; #233 ISSUE → CLOSED; ALL OTHER GITHUB MUTATIONS ZERO`

Report repository mutations exactly by path.

# Stop conditions

Stop if:
- existing WP05/WP06/WP07 symbols cannot be invoked without production refactor;
- probe requires a generic bridge or new transport;
- Streamlit listener ownership cannot be proven;
- restart requires production changes;
- Worker seam/helper must change;
- package addition required;
- finite duration cannot be met;
- regression fails;
- residue unsafe;
- WP09 boundary crossed;
- Project item identity ambiguous.

Preserve valid partial WP08 work and keep #233 Open / Backlog.

# Terminal markers

Success:

`RELEASE 1.9 WP08 FINITE-DEMONSTRATION CONSOLIDATED IMPLEMENTATION AND COMPLETION COMPLETE`

Blocked:

`RELEASE 1.9 WP08 FINITE-DEMONSTRATION CONSOLIDATED IMPLEMENTATION AND COMPLETION BLOCKED`

Do not emit COMPLETE unless the governed Python chain, Streamlit readiness/listener, restart, complete residue matrix, all regressions, and #233 Closed / Done are authoritatively proven.
