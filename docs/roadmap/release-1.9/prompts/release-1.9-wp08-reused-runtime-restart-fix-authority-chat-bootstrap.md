Execute `release-1.9-wp08-reused-runtime-restart-fix-authority-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Treat R-RUNTIME as accepted: R1 reused runtime fails with `0xC0000142`, while R1F fresh runtime passes with `0`. Freeze the Windows helper, signing setup, Worker cancellation path, probe, Replay, and WP05–WP07 semantics.
Modify only `WP08LifecycleDemonstrationTests.cs` initially. Run the minimum one-factor reuse split to isolate handoff vs DB/sidecars vs other runtime-directory residue. Do not fix until the culprit is classified. Then apply only the minimum contract-consistent cleanup/reset. A production startup-cleanup fix is allowed only if the binding contract clearly assigns that responsibility and the exact path is already authorized; otherwise stop for path authority.
Do not escape by changing final acceptance to fresh runtime. Require shared-runtime R1 to exit 0, preserve R1F/R0–R4/A–D/standalone behavior, run regressions, keep #233/#234 Open/Backlog, and make GitHub mutations zero. End with the authority's exact terminal marker.
