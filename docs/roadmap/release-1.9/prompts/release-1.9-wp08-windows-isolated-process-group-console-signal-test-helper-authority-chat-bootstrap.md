Execute `release-1.9-wp08-windows-isolated-process-group-console-signal-test-helper-authority-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Define only a Windows-specific WP08 test/demo helper that launches the real Worker in an isolated console process group and safely sends the already-selected CTRL_BREAK signal to that owned group. Preserve the existing partial Worker cancellation/liveness implementation exactly; authorize no additional production changes unless a separate amendment is required.
Fix the exact Windows console/process-group model, native APIs/flags, command-line/environment/stdio behavior, test-runner protection, cleanup fallback, Job Object decision, platform scope, and exact WP08 helper/test paths. No custom IPC, broad taskkill, Replay changes, packages, GitHub mutation, or WP09. End with the authority's exact terminal marker.
