Execute `release-1.9-wp08-windows-native-diagnostic-capture-implementation-authority-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
This is diagnostic-only and no fix is authorized. Modify only `WindowsIsolatedProcessGroup.cs` and `WP08LifecycleDemonstrationTests.cs`.
Implement factual bounded Worker stdout/stderr capture using Windows anonymous pipes, non-inheritable parent read handles, `STARTF_USESTDHANDLES`, bounded concurrent draining, exact handle-close ordering, signal result/error capture, process-group metadata, monotonic timings, and the A/B/C/D topology matrix. Do not invent console metadata that cannot be observed.
Preserve `CreateProcessW + CREATE_NEW_PROCESS_GROUP + targeted CTRL_BREAK`, Worker/Replay/Python/WP05–WP07 semantics, runner safety, and GitHub state. Run the matrix and classify the root cause H/P/S/T/W or remain U. Do not apply a behavioral fix even if the cause is obvious. Keep #233 and #234 Open/Backlog; GitHub mutations zero. End with the authority's exact terminal marker.
