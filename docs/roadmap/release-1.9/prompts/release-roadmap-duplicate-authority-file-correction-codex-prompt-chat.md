Read `release-roadmap-duplicate-authority-file-correction-codex-prompt.md` completely and execute it as the sole authority for classifying the unexpected `(1)` chat file before resuming the blocked roadmap correction.
Compare `release-roadmap-malformed-milestone-57-correction-codex-prompt-chat (1).md` completely against the canonical chat file using SHA-256/byte comparison plus narrowly normalized UTF-8 BOM, CRLF/LF, and terminal-newline comparison; require the same exact 5 non-empty instruction lines.
Only if equivalence is proven, classify `(1)` as `ACCIDENTAL_DUPLICATE_EXECUTION_INPUT` and delete exactly that file; never delete, rename, overwrite, or replace either canonical authority file, and do not commit the accidental duplicate.
Revalidate the clean corrective starting state and freshly re-read #57; if its exact malformed OPEN/0-issue/no-Project fingerprint is unchanged, resume the existing milestone #57 correction authority from its deletion gate without restarting the already-successful taxonomy or #50/#51 reconciliation.
Run DUP1–DUP20, preserve all canonical governed Markdown for the later branch commit/PR workflow, introduce no Release 1.9 implementation or unrelated cleanup, and finish with the authority's exact success or blocked markers.
