Execute `release-1.9-wp08-consolidated-implementation-completion-final-codex-prompt.md` as the sole authority using GPT-5.6 Terra.
Bind all three WP08 artifacts: lifecycle/bounded-demonstration/process-residue, Worker lifecycle/liveness cancellation adapter, and Windows isolated process-group CTRL_BREAK helper authority. Do not require a clean worktree; preserve the existing partial Worker implementation and unrelated predecessor/local work.
Implement only the authorized Windows test helper and `WP08LifecycleDemonstrationTests.cs`, with only narrow conformance edits to the already-authorized Worker cancellation/liveness files if required. Use `CreateProcessW + CREATE_NEW_PROCESS_GROUP + targeted CTRL_BREAK_EVENT`; no broad taskkill, custom IPC, Job Object, Replay change, package, or WP09 work.
Then execute the exact finite WP08 demonstration and all residue/regression gates. Require the 309/309 .NET predecessor baseline plus only authorized WP08 test deltas and all WP05–WP07 Python regressions. Close #233 / mark Done only after every acceptance, graceful-cancellation, restart, bounded-refresh, residue, scope, preservation, and authoritative GitHub read-back gate passes. Keep #234 Open / Backlog and do not start WP09. End with the authority's exact terminal marker.
