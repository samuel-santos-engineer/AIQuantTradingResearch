Execute `release-1.9-wp08-worker-lifecycle-liveness-test-seam-cancellation-adapter-contract-path-authority-definition-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Define both pieces the prior authority lacked: (1) one minimal Worker-owned standard .NET process-lifetime cancellation adapter, and (2) one narrow test/demo-only lifecycle liveness seam that makes a real Worker process stay legitimately cancellable long enough for WP08 to prove graceful cancellation without changing Replay semantics.
The seam must have production-default no-op behavior, no user-facing config, no custom IPC/listener/control file, no arbitrary production sleep or infinite loop, and must not bypass the real process-level cancellation adapter. Fix exact readiness, activation, exit/race behavior, process-level proof, and exact Program.cs/helper/WP08-test path authority. Definition/documentation only; do not implement code/tests, mutate GitHub, close #233, or start WP09. End with the authority's exact terminal marker.
