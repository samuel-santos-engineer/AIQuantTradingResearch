Execute `release-1.9-wp08-combined-lifecycle-ctrl-break-diagnostic-fix-authority-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Treat the standalone targeted CTRL_BREAK→Worker exit-0 path, reconciled P1→P2 seam, Windows process-group helper, Python probe, focused WP08 4/4, and 313/313 .NET as accepted predecessor state. Diagnose why only the combined Streamlit + probe + Worker lifecycle exits Worker A with `0xC0000142`.
Run a bounded A/B/C/D diagnostic matrix to isolate Streamlit, probe, console/process-group, stdio/inherited-handle, and timing effects. Add only bounded diagnostics needed to prove root cause. Do not apply speculative fixes.
If root cause is proven, implement the smallest fix only in the authorized harness/helper/Worker path for that class, preserving standalone CTRL_BREAK exit 0, P2 within 8 seconds, Replay semantics, WP05–WP07 semantics, runner safety, and no custom IPC/packages/WP09. Keep #233 and #234 Open/Backlog; GitHub mutations zero. End with the authority's exact terminal marker.
