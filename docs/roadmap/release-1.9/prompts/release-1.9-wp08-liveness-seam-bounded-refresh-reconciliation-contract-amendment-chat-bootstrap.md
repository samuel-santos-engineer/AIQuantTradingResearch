Execute `release-1.9-wp08-liveness-seam-bounded-refresh-reconciliation-contract-amendment-codex-prompt.md` as the sole authority using GPT-5.6 Luna.
Reconcile only the proven WP08 conflict: the current post-P1 cancellation hold prevents the required genuinely newer publication within the fixed 8-second bounded-refresh window. Preserve all valid partial WP08 production work, the 4/4 Windows/helper tests, targeted CTRL_BREAK mechanism, Replay semantics, and Worker/Streamlit independence.
Prefer an automatic two-phase test seam: P1 is a real publication and passes through; the first qualifying newer P2 is produced by normal existing execution, then the seam enters the existing cancellation-aware hold. Define exact P1/P2 readiness, newer-publication source, 8-second timing, race behavior, per-process seam state, cancellation readiness, exact path/symbol amendment, and future assertions. No harness release IPC, new config/env setting, Replay/tick/publication changes, Windows-helper redesign, packages, GitHub mutation, or WP09. End with the authority's exact terminal marker.
