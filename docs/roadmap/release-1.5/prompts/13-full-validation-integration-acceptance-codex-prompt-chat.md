Read `13-full-validation-integration-acceptance-codex-prompt.md` completely and treat it as the sole WP13 authority, reconciled with the Release 1.5 definition/plan/manifest, all accepted WP01–WP12 prompts/completions, experiment semantic/identity authorities, cumulative implementation/tests/docs, and formally closed Release 1.4 baseline.
After verifying #179 Closed/Done and #180 Open/Backlog, move only #180 to In Progress and perform a strict no-staging pre-integration reconciliation: exact candidate paths/categories, actual executed prompt names, one prompt companion each, every governed companion exactly five non-empty lines, out-of-band exclusions, direct whitespace, residue, semantic/identity/failure/DI/Worker acceptance, Releases 1.1–1.4 regressions, architecture/schema/package/reference/docs/security gates, and 238/238 permanent tests.
Do not stage anything unless every pre-staging gate passes; if they do, create only `release/1.5-deterministic-research-experiment-foundation`, stage exactly the governed candidate, commit once with `feat: establish Release 1.5 deterministic research experiment foundation`, rerun validation, prove the exact commit from a fresh detached checkout, then push normally and create one non-draft unmerged PR to `main` with auto-merge disabled.
On successful PR read-back, post evidence to #180, close it, set it Done, but leave milestone #46 OPEN, preserve all branches, create no tag/release, do not merge, and do not begin Release 1.6; stop rather than guessing on any governance/candidate/semantic/integration discrepancy.
Report the complete acceptance/integration evidence and finish with `RELEASE 1.5 WP13 COMPLETE` followed by `NEXT AUTHORIZED LIFECYCLE ACTION: Human review and explicit merge authorization for the Release 1.5 integration PR. The PR must remain unmerged and milestone #46 must remain open.`, or the required blocked marker.
