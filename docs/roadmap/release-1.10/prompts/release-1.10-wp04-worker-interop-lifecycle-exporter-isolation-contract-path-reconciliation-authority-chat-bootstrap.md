Execute `release-1.10-wp04-worker-interop-lifecycle-exporter-isolation-contract-path-reconciliation-authority-codex-prompt.md`.

## Model assignment
- **GPT-5.6 Luna** — PRIMARY contract/path/dependency/lifecycle reconciliation authority.
- **GPT-5.6 Terra** — downstream implementation only after Luna freezes the contract.
- **GPT-5.6 Sol** — supporting analysis only.

Use **GPT-5.6 Luna**.

WP04 #245 is Open/Backlog and the first Terra authority correctly blocked with zero mutations because these were deferred:
- exporter technology;
- package IDs/versions/project ownership;
- exact Worker/interop production symbols;
- exact lifecycle-test paths;
- initialization/flush/disposal;
- disabled/failure configuration;
- WP05 handoff.

Resolve all of them without implementing WP04.

Read the Release 1.10 definition, execution plan, file manifest, `OPEN_TELEMETRY_SELECTION.md`, #245, current Worker/interop code/tests/projects/package graph, WP03 contract, and WP05 planning requirements.

Freeze exact:
1. exporter selection and rejected alternatives;
2. package IDs/versions/project file, or explicitly zero package changes;
3. production paths/symbols;
4. lifecycle owner/init/shutdown/disposal/flush semantics;
5. configuration/default/invalid semantics;
6. exporter failure isolation;
7. resource/cardinality/security contract;
8. interop/no-bypass behavior;
9. test paths;
10. WP05 handoff.

Update only the minimum Release 1.10 planning/architecture docs. Production/test/project/package mutations ZERO. Git/GitHub mutations ZERO. #245 remains Open/Backlog.

Before completion, simulate Terra's implementation path. Terra must have no remaining architectural choice.

Required readiness:
`RELEASE 1.10 WP04 MATERIALIZATION SIMULATION: PASS — TERRA-READY`
`RELEASE 1.10 WP04 CONTRACT/PATH RECONCILIATION: PASS`
`RELEASE 1.10 WP04 → TERRA IMPLEMENTATION HANDOFF: PASS`

End only with exact Luna reconciliation COMPLETE or BLOCKED terminal.
