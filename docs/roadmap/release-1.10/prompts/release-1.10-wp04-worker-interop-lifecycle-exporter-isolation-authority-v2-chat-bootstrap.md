Resume WP04 implementation using `release-1.10-wp04-worker-interop-lifecycle-exporter-isolation-authority-v2-codex-prompt.md`.

## Model assignment
- **GPT-5.6 Luna** — frozen contract/architecture/reconciliation/governance.
- **GPT-5.6 Terra** — PRIMARY WP04 implementation, validation, and lifecycle completion authority.
- **GPT-5.6 Sol** — supporting analysis only.

Use **GPT-5.6 Terra**.

Luna reconciliation is COMPLETE and TERRA-READY.

Frozen facts:
- no external exporter for Release 1.10;
- zero exporter/package/project-file mutations;
- BCL-only Worker/interop observations;
- exact Worker, Program, Python invoker, and focused test paths are recorded in the reconciled planning artifacts;
- Worker owns lifecycle;
- Streamlit remains independent;
- canonical JSON handoff and schema v4 remain unchanged;
- WP05 handoff is frozen;
- #245 remains Open/Backlog.

Do not reopen exporter/package/lifecycle architecture choices. Read the three reconciled planning/architecture artifacts and implement only the exact frozen paths/symbols.

Use approved local Worker dev signing only for generated artifacts if Windows Application Control requires it.

Use Gitleaks 8.30.1 with canonical:
`gitleaks git . --redact --verbose`
without weakening PowerShell/Windows policy.

After exact:
`RELEASE 1.10 WP04 ACCEPTANCE: PASS`

close #245 and ensure its unique Project #2 item is Done. If Project automation performs Done automatically, do not issue a redundant Status mutation.

Keep milestone #59 Open. Leave #246–#249 unchanged. Git mutations ZERO. Do not start WP05.

End only with exact WP04 V2 COMPLETE or BLOCKED terminal.
