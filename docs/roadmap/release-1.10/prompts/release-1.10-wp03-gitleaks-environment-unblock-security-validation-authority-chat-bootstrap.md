Execute `release-1.10-wp03-gitleaks-environment-unblock-security-validation-authority-codex-prompt.md`.

## Model assignment
- **GPT-5.6 Luna** — frozen contract/governance/acceptance authority.
- **GPT-5.6 Terra** — execute this narrow Gitleaks environment-unblock/security-validation authority.
- **GPT-5.6 Sol** — supporting analysis only.

Use **GPT-5.6 Terra**.

Purpose: resolve only the Windows access-control block preventing the approved `gitleaks.exe` security gate from executing.

Accepted WP03 evidence remains:
- focused listener tests 25/25 PASS;
- topology parent proof PASS;
- Infrastructure 184/184 PASS;
- Application 131/131 PASS;
- Architecture 21/21 PASS using signed `--no-build`;
- Git/GitHub mutations ZERO;
- #244 Open/Backlog.

Allowed:
- inspect approved Gitleaks identity/version/path;
- use the existing documented repository/local tooling bootstrap;
- restore/reacquire the exact approved Gitleaks binary through an already-established workflow;
- run the exact required Gitleaks scan.

Forbidden:
- WP03 source/test edits;
- `.csproj`/package/schema changes;
- changing tracked security contracts;
- disabling/weaking Windows security controls;
- broad exclusions;
- unapproved alternative scanners;
- Git/GitHub mutations;
- closing #244;
- starting WP04.

Required success:
1. approved Gitleaks execution path restored;
2. access-control denial gone;
3. exact WP03 security scan executes;
4. zero unresolved secrets findings;
5. zero tracked repository-contract mutations;
6. hand back to SAME WP03 V2 final-acceptance authority.

Do NOT emit WP03 final acceptance here.

End only with the exact Gitleaks-unblock COMPLETE or BLOCKED terminal marker.
