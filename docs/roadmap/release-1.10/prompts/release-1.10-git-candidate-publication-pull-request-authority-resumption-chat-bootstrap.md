Execute `release-1.10-git-candidate-publication-pull-request-authority-resumption-codex-prompt.md`.

Use **GPT-5.6 Terra**.

Luna reconciliation is COMPLETE and TERRA-READY.

Binding superseding literals:
- canonical base / required candidate parent:
  `5cc2d17d3d05f84911eca98d3b7b7a9b33f55a33`
- raw worktree inventory: 105 paths
- canonical publication candidate: 103 paths
  - 21 tracked
  - 82 untracked before staging
  - 70 prompt artifacts
- INCLUDE the two Git Candidate Publication & Pull Request authority prompt files.
- EXCLUDE the two Remote Base & Publication-Authority Artifact Reconciliation authority files.
- exact literal candidate/exclusion set is persisted in the Release 1.10 manifest.
- exact re-anchor/materialization and full validation policy is persisted in the execution plan.
- #242–#249 Closed/Done.
- milestone #59 Open, 0 open / 8 closed.
- prior publication and Luna reconciliation performed ZERO Git/GitHub publication mutations.

The reconciled manifest/plan supersede only stale `35ec...`, 101-path, 21+80, and 68-prompt literals from the original Terra authority. Preserve all other constraints.

First execute the frozen re-anchor/full-validation policy. Then stage exactly the reconciled 103 paths, commit from parent `5cc2...`, push the governed branch, create/reuse exactly one Release 1.10 PR, and post-verify.

Do NOT stage the two excluded reconciliation authority files.

Do NOT modify candidate content, merge, force-push, close milestone #59, tag/version, or publish a GitHub Release.

End only with the exact COMPLETE or BLOCKED terminal.
