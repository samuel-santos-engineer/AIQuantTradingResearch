Execute `release-1.10-wp03-infrastructure-instrumentation-contract-path-reconciliation-authority-codex-prompt.md` as the narrow Luna reconciliation for the blocked WP03 Terra authority.

## Model assignment
- **GPT-5.6 Luna** — execute now; contract, architecture, exact path/symbol ownership, telemetry vocabulary, dependency/API decision, and tests.
- **GPT-5.6 Terra** — reserved for resumed WP03 implementation/validation/GitHub completion.
- **GPT-5.6 Sol** — supporting analysis/synthesis/non-authoritative review.

Use **GPT-5.6 Luna**.

WP03 #244 is still Open / Backlog. No WP03 implementation, Git, or GitHub mutation occurred.

Read the current Release 1.10 planning artifacts, `OPEN_TELEMETRY_SELECTION.md`, #244, accepted WP02 implementation, all relevant Infrastructure implementations/project files, and existing Infrastructure tests.

Resolve deterministically:
- exact production paths and writable symbols;
- classification of `SqliteHistoricalObservationStore`, `SqliteDatasetSnapshotStore`, and `SqliteDatasetCatalog`;
- exact dedicated test paths;
- Infrastructure ActivitySource/Meter ownership and names;
- exact activities and real measured intervals;
- exact metrics/types/units;
- bounded attributes and failure categories;
- exact WP02 parent topology;
- explicit BCL-only vs package-backed decision;
- exact package/project paths/versions only if package-backed;
- exact WP04 handoff.

Prefer BCL-only if it fully satisfies WP03 while preserving WP04 SDK/exporter ownership.

Planning/contract mutations only.
Production/test/package mutations: ZERO.
Git mutations: ZERO.
GitHub mutations: ZERO.

Require a Terra materialization simulation proving all implementation questions are answerable without invention.

On PASS, regenerate/resume:
**Release 1.10 WP03 — Infrastructure Provider, Persistence & Failure Instrumentation Authority — GPT-5.6 Terra**

End only with the exact COMPLETE or BLOCKED terminal marker.
