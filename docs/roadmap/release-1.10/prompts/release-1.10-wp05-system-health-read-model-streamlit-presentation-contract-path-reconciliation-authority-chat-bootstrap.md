Execute `release-1.10-wp05-system-health-read-model-streamlit-presentation-contract-path-reconciliation-authority-codex-prompt.md`.

## Model assignment
- **GPT-5.6 Luna** — PRIMARY WP05 contract/path/read-model/handoff/presentation reconciliation authority.
- **GPT-5.6 Terra** — downstream implementation only after deterministic Luna TERRA-READY.
- **GPT-5.6 Sol** — supporting analysis only.

Use **GPT-5.6 Luna**.

The first WP05 Terra authority correctly BLOCKED before mutation. `aiq-visualization-read-model-v1` currently supports only `Ready`, `WarmUp`, `Empty`, `Stale`, and `Failed`. Governing artifacts do not yet freeze the System Health extension, exact .NET source/read-model symbols, health timestamp/freshness semantics, compatibility behavior, Python/Streamlit production/test allowlists, or WP06 handoff. The manifest explicitly defers WP05 paths.

Reconcile without implementation.

Read the Release 1.10 definition, execution plan, file manifest, `OPEN_TELEMETRY_SELECTION.md`, #246/#247, current visualization read model/handoff, WP03/WP04 observability/lifecycle, Python parser/frame/presentation, Streamlit, and all relevant tests.

Freeze exact:
1. visualization-state vs System-Health separation;
2. bounded System Health vocabulary;
3. .NET source of truth;
4. .NET paths/symbols;
5. canonical JSON health shape;
6. `aiq-visualization-read-model-v1` version/compatibility rule;
7. timestamp/freshness semantics;
8. provenance disclosure;
9. Python/Streamlit paths/symbols;
10. Streamlit presentation mapping;
11. missing/malformed/pre-WP05 behavior;
12. exact test allowlist;
13. security/cardinality/no-bypass;
14. WP06 permanent-test handoff.

Do not casually create read-model v2, a second health channel, a new SQLite schema, an external exporter, or a Python/Streamlit health authority.

Update only the minimum authorized Release 1.10 planning/architecture docs. Production/test/project/package/schema, Git, and GitHub mutations ZERO. #246 remains Open/Backlog. Milestone #59 stays Open.

Before completion, simulate Terra's exact end-to-end implementation. Terra must have no remaining architectural choice.

Required readiness:
`RELEASE 1.10 WP05 MATERIALIZATION SIMULATION: PASS — TERRA-READY`
`RELEASE 1.10 WP05 CONTRACT/PATH RECONCILIATION: PASS`
`RELEASE 1.10 WP05 → TERRA IMPLEMENTATION HANDOFF: PASS`

End only with the exact Luna reconciliation COMPLETE or BLOCKED terminal.
