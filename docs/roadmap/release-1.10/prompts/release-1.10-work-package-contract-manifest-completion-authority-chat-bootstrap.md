Execute `release-1.10-work-package-contract-manifest-completion-authority-codex-prompt.md` as the sole authority.

## Model assignment
- **GPT-5.6 Luna** — execute now; contract, architecture, scope allocation, acceptance, governance, reconciliation.
- **GPT-5.6 Terra** — reserve for later implementation, validation execution, Git/GitHub mutations, merge/publication.
- **GPT-5.6 Sol** — supporting analysis, synthesis, alternatives, non-authoritative review.

Use **GPT-5.6 Luna**.

The prior Terra GitHub planning materialization authority correctly BLOCKED because the accepted Release 1.10 artifacts contain the eight WP names/order but lack deterministic per-WP contracts.

Read:
- `docs/roadmap/release-1.10/RELEASE_1.10_DEFINITION.md`
- `docs/roadmap/release-1.10/RELEASE_1.10_EXECUTION_PLAN.md`
- `docs/roadmap/release-1.10/RELEASE_1.10_FILE_MANIFEST.md`

Freeze the exact eight WP identities/order. Without changing release capability, complete every WP with exact objective, in/out scope, architecture and provenance constraints, direct dependency edges, bounded repository/path ownership, measurable acceptance criteria, validation requirements, security requirements, Luna/Terra/Sol assignment, selected execution model, and completion boundary.

Build an acyclic dependency topology, cross-WP responsibility matrix, release acceptance coverage matrix, and schema/protocol/dependency ownership mapping. No release requirement may remain orphaned or ambiguously owned.

Update only the minimum Release 1.10 planning documents under `docs/roadmap/release-1.10/`. No implementation/source/test/package/schema/runtime changes. Zero Git mutations. Zero GitHub mutations.

Before completion, simulate deterministic GitHub issue materialization and require all eight WPs to be `READY FOR DETERMINISTIC ISSUE MATERIALIZATION`. If Terra would still need to invent planning detail, BLOCK.

On PASS, the next authority is the existing **Release 1.10 GitHub Planning Materialization Authority — GPT-5.6 Terra**, which must rerun its idempotence gate.

End only with the exact COMPLETE or BLOCKED terminal marker from the full authority.
