Resume the SAME WP03 V2 authority with `release-1.10-wp03-v2-test-proof-completion-authority-codex-prompt.md`.

## Model assignment
- **GPT-5.6 Luna** — frozen contract/architecture/reconciliation/governance.
- **GPT-5.6 Terra** — execute now; finish the remaining WP03 test/proof work, full validation, acceptance, and approved GitHub completion.
- **GPT-5.6 Sol** — supporting analysis/synthesis only.

Use **GPT-5.6 Terra**.

Accepted current state:
- `RELEASE 1.10 WP03 EXIT-PATH OBSERVABILITY MATRIX: FROZEN`
- all normal return paths of the three authorized production methods route through in-scope completion logic;
- Infrastructure build 0 warnings / 0 errors;
- existing focused baseline 23/23;
- no forbidden/package/project/schema/Git/GitHub mutations;
- #244 remains Open/Backlog.

Do not rewrite valid production work unnecessarily.

Remaining primary task: add deterministic `ActivityListener` and `MeterListener` proof only in the two authorized existing test files:
- `SqlitePersistenceTests.cs`
- `SqliteDatasetTests.cs`

Prove exact source/meter, `provider.operation`, `persistence.operation`, exact frozen metrics/types/units, bounded attributes/outcomes/failure categories, failure semantics, WP02→WP03 topology, no catalog duplication, no Persist instrumentation, and no high-cardinality/sensitive dimensions.

Then run full affected Infrastructure/Application/architecture/security/residue/functional/path-hunk validation.

Do not block merely because these tests/validations still need to be performed.

Git mutations: ZERO.

Before `RELEASE 1.10 WP03 ACCEPTANCE: PASS`: GitHub mutations ZERO.

After acceptance only:
1. close #244;
2. set its unique Project #2 item Status to Done.

Keep milestone #59 Open and #245–#249 unchanged.

Do not start WP04.

End only with exact WP03 V2 COMPLETE or BLOCKED terminal marker.
