Read `11-application-infrastructure-discovery-tests-codex-prompt.md` completely and execute it as the sole authority for Release 1.7 WP11 — Application & Infrastructure Discovery Tests, GitHub issue #207.
Add only focused permanent repository-native Application/Infrastructure regressions for request validation, one-call orchestration, empty/non-empty pass-through, evidence fidelity, dual-identity filtering, binary ordering, bounded maximum, read-only behavior, failures, unknown defects, and manifest-owned DI/process integration using `TemporaryDatabase`, production seeding, and `--no-build` Worker execution.
Do not corrupt durable state for `IntegrityConflict`, duplicate predecessor coverage, inflate counts, modify production/schema/SQL/DI/Worker behavior, add packages/projects/references, perform Git transport, or begin the Release 1.8 review.
Run targeted changed-project tests and full canonical verification, report exact Domain/Application/Infrastructure/Architecture before → after deltas and the truthful new permanent baseline, and require Architecture 13/13, build 0 warnings/errors, formatting/Gitleaks/diff/whitespace/offline/credential/residue PASS with production delta 0; only after every gate passes close #207 Done while leaving #208–#209 Open/Backlog.
On success end exactly with `RELEASE 1.7 WP11 COMPLETE` and `NEXT AUTHORIZED WORK PACKAGE: WP12 — Architecture & Documentation Alignment — GitHub issue #208`.
